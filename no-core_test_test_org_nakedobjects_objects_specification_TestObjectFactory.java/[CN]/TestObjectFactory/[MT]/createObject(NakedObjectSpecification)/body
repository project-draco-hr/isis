{
  if (specification.getShortName().equals("movie")) {
    return new Movie();
  }
 else   if (specification.getShortName().equals("person")) {
    return new Person();
  }
  throw new NakedObjectRuntimeException("Unknown class: " + specification.getFullName());
}
