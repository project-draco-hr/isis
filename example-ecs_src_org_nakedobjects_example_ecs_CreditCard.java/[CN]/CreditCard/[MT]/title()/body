{
  if (getNumber().isEmpty()) {
    return new Title();
  }
 else {
    String num=getNumber().stringValue();
    int pos=Math.max(0,num.length() - 5);
    return new Title("*****************".substring(0,pos)).concat(num.substring(pos));
  }
}
