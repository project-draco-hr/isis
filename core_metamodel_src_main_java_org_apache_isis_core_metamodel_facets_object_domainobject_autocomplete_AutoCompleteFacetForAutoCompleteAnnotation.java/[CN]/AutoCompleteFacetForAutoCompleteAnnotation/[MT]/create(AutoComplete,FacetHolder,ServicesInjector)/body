{
  if (annotation == null) {
    return null;
  }
  final Class<?> repositoryClass=annotation.repository();
  final String actionName=annotation.action();
  return new AutoCompleteFacetForAutoCompleteAnnotation(holder,repositoryClass,actionName,servicesInjector);
}
