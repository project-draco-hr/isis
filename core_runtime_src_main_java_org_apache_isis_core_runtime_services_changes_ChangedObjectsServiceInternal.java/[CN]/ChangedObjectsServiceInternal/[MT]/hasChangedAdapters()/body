{
  final Set<Map.Entry<AdapterAndProperty,PreAndPostValues>> changedObjectProperties=getChangedObjectProperties();
  final Set<ObjectAdapter> changedAdapters=Sets.newHashSet(Iterables.transform(changedObjectProperties,AdapterAndProperty.Functions.GET_ADAPTER));
  return !changedAdapters.isEmpty();
}
