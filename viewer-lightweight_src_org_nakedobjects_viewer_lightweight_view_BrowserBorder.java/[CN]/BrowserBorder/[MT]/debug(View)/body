{
  Padding in=getPadding(view);
  String name=getClass().getName();
  return name.substring(name.lastIndexOf('.') + 1) + " [top=" + in.getTop()+ ",left="+ in.getLeft()+ ",bottom="+ in.getBottom()+ ",right="+ in.getRight()+ "]";
}
