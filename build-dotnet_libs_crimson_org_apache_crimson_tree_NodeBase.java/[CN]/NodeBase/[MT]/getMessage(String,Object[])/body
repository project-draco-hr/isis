{
  Locale locale;
  if (this instanceof XmlDocument)   locale=((XmlDocument)this).getLocale();
 else   if (ownerDocument == null)   locale=Locale.getDefault();
 else   locale=ownerDocument.getLocale();
  return XmlDocument.catalog.getMessage(locale,messageId,parameters);
}
