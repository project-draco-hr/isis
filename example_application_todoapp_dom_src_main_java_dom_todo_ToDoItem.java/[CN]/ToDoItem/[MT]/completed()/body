{
  setComplete(true);
  @SuppressWarnings("unused") final List<Object> allObjects=actionInvocationContext.getDomainObjects();
  LOG.debug("completed: " + actionInvocationContext.getIndex() + " ["+ actionInvocationContext.getSize()+ "]"+ (actionInvocationContext.isFirst() ? " (first)" : "")+ (actionInvocationContext.isLast() ? " (last)" : ""));
  return actionInvocationContext.getInvokedOn() == InvokedOn.OBJECT ? this : null;
}
