{
  recordEvent(ev);
switch (ev.getEventPhase()) {
case HIDE:
    break;
case DISABLE:
  break;
case VALIDATE:
break;
case EXECUTING:
break;
case EXECUTED:
LOG.info("Received ToDoItem.CompletedEvent for : " + ev.getSource().toString());
break;
}
}
