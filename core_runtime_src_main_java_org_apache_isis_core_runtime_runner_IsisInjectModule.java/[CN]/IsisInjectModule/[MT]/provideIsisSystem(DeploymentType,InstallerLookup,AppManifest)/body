{
  final IsisComponentProviderUsingInstallers componentProvider=new IsisComponentProviderUsingInstallers(deploymentType,appManifestIfAny == APP_MANIFEST_NOOP ? null : appManifestIfAny,installerLookup);
  final IsisSystem system=new IsisSystem(componentProvider);
  system.init();
  return system;
}
