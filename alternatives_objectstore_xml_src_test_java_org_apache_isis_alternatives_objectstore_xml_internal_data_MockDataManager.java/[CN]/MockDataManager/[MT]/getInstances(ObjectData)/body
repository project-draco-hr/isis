{
  ObjectDataVector vector=new ObjectDataVector();
  Iterator i=actions.iterator();
  while (i.hasNext()) {
    ObjectData data=(ObjectData)i.next();
    if (pattern.getSpecification().equals(data.getSpecification()))     vector.addElement(data);
  }
  return vector;
}
