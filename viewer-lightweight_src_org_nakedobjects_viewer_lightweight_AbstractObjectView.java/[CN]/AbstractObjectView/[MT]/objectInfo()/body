{
  if (getObject() != null) {
    String status=getObject().about().getDescription();
    if (status.equals("")) {
      status=getObject().title() + " (" + getObject().getShortClassName()+ ")";
    }
    return status;
  }
 else {
    return "null";
  }
}
