{
  Bounds b=getBounds();
  String cls=getClass().getName();
  String bounds=b == null ? "unbounded" : b.width + "x" + b.height+ "+"+ b.x+ "+"+ b.y;
  return cls.substring(cls.lastIndexOf('.') + 1) + getId() + " ["+ bounds+ ",object="+ getObject()+ "]";
}
