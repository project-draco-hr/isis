{
  debug.appendln(title);
  debug.indent();
  if (vector.size() == 0) {
    debug.appendln("none");
  }
 else {
    for (    final String text : vector) {
      debug.appendln(text);
    }
  }
  debug.unindent();
}
