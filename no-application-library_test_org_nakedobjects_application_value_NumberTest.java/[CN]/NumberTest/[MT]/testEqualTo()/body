{
  IntegerNumber one=new IntegerNumber(10);
  IntegerNumber two=new IntegerNumber(11);
  IntegerNumber three=new IntegerNumber(10);
  assertTrue(one.isEqualTo(three));
  assertFalse(one.isEqualTo(two));
}
