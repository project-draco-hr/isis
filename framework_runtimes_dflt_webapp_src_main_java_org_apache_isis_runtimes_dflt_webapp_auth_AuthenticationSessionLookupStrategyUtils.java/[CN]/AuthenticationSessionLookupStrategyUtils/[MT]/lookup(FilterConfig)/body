{
  String authLookupStrategyClassName=filterConfig.getInitParameter(AuthenticationSessionLookupStrategyConstants.AUTHENTICATION_SESSION_LOOKUP_STRATEGY_KEY);
  return lookup(authLookupStrategyClassName);
}
