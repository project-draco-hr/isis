{
  Vector v=new Vector();
  for (int i=0; i < fields.length; i++) {
    MemberIf member=fields[i];
    if (member instanceof OneToOneAssociationIF && ((OneToOneAssociationIF)member).hasAddMethod()) {
      v.addElement(member.getName());
    }
  }
  String[] names=new String[v.size()];
  v.copyInto(names);
  return names;
}
