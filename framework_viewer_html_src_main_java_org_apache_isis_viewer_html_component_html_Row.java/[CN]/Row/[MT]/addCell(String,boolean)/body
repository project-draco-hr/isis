{
  String s;
  if (truncate) {
    s=string.substring(0,Math.min(TRUNCATE_LENGTH,string.length()));
    if (string.length() > TRUNCATE_LENGTH) {
      s+="...";
    }
  }
 else {
    s=string;
  }
  add(new Html(s));
}
