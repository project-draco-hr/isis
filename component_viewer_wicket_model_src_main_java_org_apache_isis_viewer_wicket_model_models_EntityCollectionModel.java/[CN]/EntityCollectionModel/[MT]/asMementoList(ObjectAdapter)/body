{
  final Iterable<Object> iterable=EntityCollectionModel.asIterable(collectionAsAdapter);
  return Lists.newArrayList(Iterables.transform(iterable,MementoFunctions.fromPojo(getAdapterManagerStatic())));
}
