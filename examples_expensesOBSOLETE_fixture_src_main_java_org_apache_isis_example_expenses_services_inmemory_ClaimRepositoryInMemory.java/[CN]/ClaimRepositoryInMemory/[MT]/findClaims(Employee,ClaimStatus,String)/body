{
  return allMatches(Claim.class,new Filter<Claim>(){
    public boolean accept(    final Claim claim){
      return (employee == null || claim.getClaimant() == employee) && (status == null || claim.getStatus() == status) && (description == null || claim.getDescription().contains(description));
    }
  }
);
}
