{
  final List<PropertyLayoutData> properties=fieldSet.getProperties();
  final ImmutableList<String> existingIds=FluentIterable.from(properties).transform(new Function<PropertyLayoutData,String>(){
    @Nullable @Override public String apply(    @Nullable final PropertyLayoutData propertyLayoutData){
      return propertyLayoutData.getId();
    }
  }
).toList();
  for (  final String propertyId : propertyIds) {
    if (!existingIds.contains(propertyId)) {
      properties.add(new PropertyLayoutData(propertyId));
    }
  }
}
