{
  Document doc=element.getOwnerDocument();
  if (doc == null) {
    return element;
  }
  Element rootElement=doc.getDocumentElement();
  if (rootElement == null) {
    return element;
  }
  return rootElement;
}
