{
  nextLogIdToWrite=Util.logFileRange().getLast();
  startNewFile();
  if (!startNewFile) {
    final File file=Util.logFile(nextLogIdToWrite);
    openFile(file);
  }
 else {
    openNewFile();
  }
}
