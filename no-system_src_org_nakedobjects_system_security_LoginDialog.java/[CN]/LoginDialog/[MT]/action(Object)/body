{
  if (widget == cancel) {
    cancel(widget);
  }
 else   if (widget == login || widget == password) {
    logIn=true;
    notify();
  }
 else   if (widget == user) {
    password.requestFocus();
  }
}
