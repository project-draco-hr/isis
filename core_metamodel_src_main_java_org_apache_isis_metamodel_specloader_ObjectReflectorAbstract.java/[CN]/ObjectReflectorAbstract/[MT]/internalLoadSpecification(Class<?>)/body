{
  Class<?> substitutedType=getClassSubstitutor().getClass(type);
  ObjectSpecification objectSpecification=substitutedType != null ? loadSpecificationForSubstitutedClass(substitutedType) : null;
  return objectSpecification;
}
