{
  if (isEmpty(path) && isEmpty(suffix)) {
    return "";
  }
  if (isEmpty(path)) {
    return suffix;
  }
  if (isEmpty(suffix)) {
    return path;
  }
  if (path.endsWith("/") || suffix.startsWith("/")) {
    return path + suffix;
  }
  return path + "/" + suffix;
}
