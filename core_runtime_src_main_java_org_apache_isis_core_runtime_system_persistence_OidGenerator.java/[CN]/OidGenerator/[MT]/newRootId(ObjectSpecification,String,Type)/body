{
  final State state=spec.containsDoOpFacet(ViewModelFacet.class) ? State.VIEWMODEL : type == Type.TRANSIENT ? State.TRANSIENT : State.PERSISTENT;
  final ObjectSpecId objectSpecId=spec.getSpecId();
  return new RootOid(objectSpecId,identifier,state);
}
