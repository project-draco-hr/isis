{
  if (!equals(other)) {
    return Comparison.NOT_EQUIVALENT;
  }
  if (getVersion() == null || other.getVersion() == null) {
    return Comparison.EQUIVALENT_BUT_NO_VERSION_INFO;
  }
  return getVersion().equals(other.getVersion()) ? Comparison.EQUIVALENT_AND_UNCHANGED : Comparison.EQUIVALENT_BUT_CHANGED;
}
