{
  TransferableReader reader=new TestReader();
  TransferableTestObject object=(TransferableTestObject)reader.readObject();
  assertEquals(34,object.getInt());
  assertEquals("smith",object.getString());
}
