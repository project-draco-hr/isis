{
  log("execute client action " + indentedNewLine() + "persisted: "+ dump(persisted)+ indentedNewLine()+ "changed: "+ dump(changed)+ indentedNewLine()+ "deleted: "+ dump(deleted));
  ObjectData[] data=decorated.executeClientAction(session,persisted,changed,deleted);
  log(" <-- data: " + dump(data));
  return data;
}
