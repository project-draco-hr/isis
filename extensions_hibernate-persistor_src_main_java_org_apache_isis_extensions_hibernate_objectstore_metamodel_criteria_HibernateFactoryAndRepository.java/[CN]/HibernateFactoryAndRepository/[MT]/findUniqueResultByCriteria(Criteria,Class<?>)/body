{
  final List<?> list=findByCriteria(new HibernateCriteriaCriteria(cls,criteria,HibernateInstancesCriteria.UNIQUE_RESULT),cls);
  if (list.size() == 1) {
    return list.get(0);
  }
  return null;
}
