{
  init();
  final ObjectAdapter serviceAdapter=getServiceAdapter(serviceId);
  ResourceContext resourceContext=getResourceContext();
  AbstractReprBuilder<?> builder=DomainObjectReprBuilder.newBuilder(resourceContext).usingLinkToBuilder(new DomainServiceLinkToBuilder()).withAdapter(serviceAdapter);
  return responseOfOk(RepresentationType.DOMAIN_OBJECT,Caching.ONE_DAY,builder).build();
}
