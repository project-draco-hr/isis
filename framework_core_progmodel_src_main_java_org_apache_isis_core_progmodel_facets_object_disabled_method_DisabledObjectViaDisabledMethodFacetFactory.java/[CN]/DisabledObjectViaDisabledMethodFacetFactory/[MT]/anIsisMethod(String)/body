{
  List<String> names=Arrays.asList("choices","clear","created","default","hide","validate","disable","iconName","modify","set");
  for (  String prefix : names) {
    if (name.startsWith(prefix)) {
      return true;
    }
  }
  return false;
}
