{
  if (parentSpec == null) {
    return;
  }
  final LinkBuilder parentLinkBuilder=DomainTypeReprRenderer.newLinkToBuilder(resourceContext,Rel.UP,parentSpec);
  getLinks().arrayAdd(parentLinkBuilder.build());
}
