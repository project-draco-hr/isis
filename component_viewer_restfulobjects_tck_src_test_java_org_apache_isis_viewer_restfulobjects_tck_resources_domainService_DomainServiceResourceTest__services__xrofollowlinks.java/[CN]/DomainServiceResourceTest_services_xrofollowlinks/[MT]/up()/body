{
  request=client.createRequest(RestfulHttpMethod.GET,"user").withArg(RequestParameter.FOLLOW_LINKS,"links[rel=" + Rel.UP.getName() + "]");
  restfulResponse=request.executeT();
  repr=restfulResponse.getEntity();
  assertThat(repr.getUp().getValue(),is(not(nullValue())));
}
