{
  writer.print("<div class=\"disabled\" title=\"");
  writer.print(reasonDisabled);
  writer.print("\">");
  writer.print(actionName);
  if (takesParameters) {
    writer.print(". . .");
  }
  writer.print("</div>");
}
