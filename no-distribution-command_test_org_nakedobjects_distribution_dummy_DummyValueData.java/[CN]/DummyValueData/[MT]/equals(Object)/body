{
  if (obj == this) {
    return true;
  }
  if (obj instanceof DummyValueData) {
    DummyValueData ref=(DummyValueData)obj;
    return (value == null ? ref.value == null : value.equals(ref.value)) && (type == null ? ref.type == null : type.equals(ref.type));
  }
  return false;
}
