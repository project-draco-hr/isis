{
  String rls="";
  for (int i=0; i < roles.length; i++) {
    rls+=roles[i].getName().stringValue() + " ";
  }
  return "SecurityContext [user=" + (user == null ? "none" : user.getName().stringValue()) + ",roles="+ rls+ "]";
}
