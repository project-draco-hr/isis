{
  if (buf.length() > 0) {
    buf.append(" ");
  }
  buf.append("col-").append(size.toCssClassFragment()).append("-").append(offset != null && offset ? "offset-" : "").append(span);
  return buf;
}
