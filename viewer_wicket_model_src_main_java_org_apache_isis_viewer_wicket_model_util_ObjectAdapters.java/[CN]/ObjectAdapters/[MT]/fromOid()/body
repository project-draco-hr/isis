{
  return new Function<Oid,ObjectAdapter>(){
    @Override public ObjectAdapter apply(    final Oid from){
      final ObjectAdapter adapterFor=getAdapterManager().getAdapterFor(from);
      return adapterFor;
    }
  }
;
}
