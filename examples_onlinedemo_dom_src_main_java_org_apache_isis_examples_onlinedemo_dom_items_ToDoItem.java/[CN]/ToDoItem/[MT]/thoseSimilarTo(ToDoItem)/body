{
  return new Filter<ToDoItem>(){
    @Override public boolean accept(    ToDoItem eachToDoItem){
      return Objects.equal(toDoItem.getCategory(),eachToDoItem.getCategory()) && eachToDoItem != toDoItem;
    }
  }
;
}
