{
  super(wrappedView);
  listSpecification=new SelectionListSpecification(){
    @Override protected View createElementView(    final Content content){
      return spec.createView(content,null,0);
    }
  }
;
}
