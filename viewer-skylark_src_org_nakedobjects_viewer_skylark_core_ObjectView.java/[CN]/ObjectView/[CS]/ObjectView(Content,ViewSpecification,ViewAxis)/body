{
  super(content,design,axis);
  if (!(content instanceof ObjectContent)) {
    throw new IllegalArgumentException("Content must be ObjectContent or AssociateContent");
  }
  getViewManager().addToNotificationList(this);
}
