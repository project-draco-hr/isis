{
  if (o instanceof Image) {
    if (index == 0) {
      return "Image";
    }
 else {
      return getOutputStreamFromImage((Image)o);
    }
  }
 else {
    throw new PersistFailedException("Invalid object type " + o.getClass().getCanonicalName() + " for JdbcImageValueMapper");
  }
}
