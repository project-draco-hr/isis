{
  final ObjectAdapter objectAdapter=getObjectAdapter(oidStr);
  final AbstractRepresentationBuilder<?> builder=DomainObjectRepBuilder.newBuilder(getResourceContext()).withAdapter(objectAdapter);
  return responseOfOk(jsonFrom(builder));
}
