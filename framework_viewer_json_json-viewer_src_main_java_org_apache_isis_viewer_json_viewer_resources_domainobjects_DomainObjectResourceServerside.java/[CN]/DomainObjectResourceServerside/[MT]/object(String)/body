{
  final ObjectAdapter objectAdapter=getObjectAdapter(oidStr);
  final RepresentationBuilder<?> builder=DomainServiceRepBuilder.newBuilder(getResourceContext()).withAdapter(objectAdapter);
  return responseOfOk(jsonFrom(builder));
}
