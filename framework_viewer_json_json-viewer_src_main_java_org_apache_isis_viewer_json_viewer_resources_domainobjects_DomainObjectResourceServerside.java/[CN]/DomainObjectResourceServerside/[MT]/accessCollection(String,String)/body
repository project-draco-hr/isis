{
  final ObjectAdapter objectAdapter=getObjectAdapter(oidStr);
  final OneToManyAssociation collection=getCollectionThatIsVisibleAndUsable(getResourceContext(),objectAdapter,collectionId,Intent.ACCESS);
  final ObjectCollectionRepBuilder builder=ObjectCollectionRepBuilder.newBuilder(getResourceContext(),objectAdapter,collection);
  return responseOfOk(jsonFrom(builder));
}
