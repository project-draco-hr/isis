{
  final ObjectAdapter objectAdapter=getObjectAdapter(oidStr);
  final ObjectAction action=getObjectActionThatIsVisibleAndUsable(getResourceContext(),objectAdapter,actionId,Intent.ACCESS);
  ObjectActionRepBuilder builder=ObjectActionRepBuilder.newBuilder(getResourceContext(),objectAdapter,action);
  return responseOfOk(jsonFrom(builder));
}
