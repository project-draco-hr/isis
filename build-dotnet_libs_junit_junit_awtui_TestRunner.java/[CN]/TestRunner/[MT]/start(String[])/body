{
  String suiteName=processArguments(args);
  fFrame=createUI(suiteName);
  final int margin=20;
  fFrame.setLocation(margin,margin);
  fFrame.setSize(1024 - (2 * margin),768 - (2 * margin));
  fFrame.setVisible(true);
  if (suiteName != null) {
    setSuiteName(suiteName);
    runSuite();
  }
}
