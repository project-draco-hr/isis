{
  Money total=new Money(0,"USD");
  Iterator<OrderItem> iterator=getItems().iterator();
  while (iterator.hasNext()) {
    OrderItem item=iterator.next();
    total=total.add(item.getTotal());
  }
  return total;
}
