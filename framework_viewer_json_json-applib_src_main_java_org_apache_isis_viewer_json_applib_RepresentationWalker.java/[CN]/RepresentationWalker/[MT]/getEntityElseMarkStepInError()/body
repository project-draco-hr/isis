{
  try {
    return getEntity();
  }
 catch (  Exception e) {
    Step previousStep=currentStep();
    previousStep.error="exception: " + e.getMessage();
    previousStep.exception=e;
    return null;
  }
}
