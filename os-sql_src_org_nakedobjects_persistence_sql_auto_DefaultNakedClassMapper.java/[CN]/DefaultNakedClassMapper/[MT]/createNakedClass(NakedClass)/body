{
  LOG.debug("saving naked class: " + cls);
  String statement="insert into " + table + " ("+ idColumn+ ", "+ nameColumn+ ", "+ reflectorColumn+ ") values ("+ primaryKey(cls.getOid())+ ",'"+ cls.getName().stringValue()+ "','"+ cls.getReflector().stringValue()+ "')";
  db.update(statement);
  cls.setResolved();
}
