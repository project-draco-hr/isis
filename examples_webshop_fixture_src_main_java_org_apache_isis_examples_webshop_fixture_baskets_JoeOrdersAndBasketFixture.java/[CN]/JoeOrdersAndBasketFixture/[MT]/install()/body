{
  final Customer joe=customers.findByEmail("joe@bloggs.com");
  Basket basket=baskets.basketFor(joe);
  addItem(basket,0,2);
  addItem(basket,1,1);
  addItem(basket,2,1);
  @SuppressWarnings("unused") final Order order1=basket.checkout();
  addItem(basket,4,1);
  addItem(basket,7,2);
  @SuppressWarnings("unused") final Order order2=basket.checkout();
  addItem(basket,3,1);
}
