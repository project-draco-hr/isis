{
  return new TypeSafeMatcher<Oid>(){
    @Override public boolean matchesSafely(    Oid item){
      return !item.isTransient();
    }
    @Override public void describeTo(    Description description){
      description.appendText("is persistent");
    }
  }
;
}
