{
  LOG.debug("Journal " + action + " - "+ writer);
  try {
    journal.writeUTF(action);
    journal.write(writer.getBinaryData());
    journal.flush();
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
