{
  if (hasFocus()) {
    return true;
  }
  View[] subviews=getSubviews();
  for (int i=0; i < subviews.length; i++) {
    if (subviews[i].containsFocus()) {
      return true;
    }
  }
  return false;
}
