{
  if (!getPeer().isList()) {
    return null;
  }
  captureCurrent(aliasAs,title,type);
  try {
    getPeer().findAndAlias();
  }
 catch (  ScenarioBoundValueException e) {
    return e.getMessage();
  }
  return null;
}
