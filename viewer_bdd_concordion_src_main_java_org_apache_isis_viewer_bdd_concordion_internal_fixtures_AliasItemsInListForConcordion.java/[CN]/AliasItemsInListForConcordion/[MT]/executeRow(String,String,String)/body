{
  if (!getPeer().isList()) {
    return null;
  }
  captureCurrent(aliasAs,title,type);
  try {
    getPeer().findAndAlias();
  }
 catch (  final ScenarioBoundValueException e) {
    return e.getMessage();
  }
  return null;
}
