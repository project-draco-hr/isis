{
  AbstractSpecification spec=new JavaSpecification(JavaObjectMarkedAsTransient.class,new DummyBuilder(),new JavaReflector());
  spec.introspect(builder);
  assertEquals(Persistable.TRANSIENT,spec.persistable());
}
