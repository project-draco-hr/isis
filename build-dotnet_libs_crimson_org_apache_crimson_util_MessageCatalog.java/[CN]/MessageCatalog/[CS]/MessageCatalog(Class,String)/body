{
  int index;
  bundleName=packageMember.getName();
  index=bundleName.lastIndexOf('.');
  if (index == -1)   bundleName="";
 else   bundleName=bundleName.substring(0,index) + ".";
  bundleName=bundleName + "resources." + bundle;
}
