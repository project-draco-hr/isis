{
  final int length=annotation.integer() + annotation.fraction();
  final int scale=annotation.fraction();
  return annotation == null ? null : new BigDecimalFacetForParameterFromJavaxValidationDigitsAnnotation(holder,length,scale);
}
