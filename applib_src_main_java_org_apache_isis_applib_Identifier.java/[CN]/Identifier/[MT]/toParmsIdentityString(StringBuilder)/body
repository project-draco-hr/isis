{
  if (type == Type.ACTION) {
    buf.append('(');
    for (int i=0; i < parameterNames.length; i++) {
      if (i > 0) {
        buf.append(",");
      }
      buf.append(parameterNames[i]);
    }
    buf.append(')');
  }
  return buf;
}
