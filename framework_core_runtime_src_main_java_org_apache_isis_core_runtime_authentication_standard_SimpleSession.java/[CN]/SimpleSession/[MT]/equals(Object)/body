{
  if (obj == this) {
    return true;
  }
  if (obj == null) {
    return false;
  }
  if (obj.getClass() != getClass()) {
    return false;
  }
  SimpleSession other=(SimpleSession)obj;
  return equals(other);
}
