{
  viewer.translate(me);
  mouseLocation=new Location(me.getPoint());
  if (canDrag) {
    if (drag == null) {
      dragStart(me);
      redraw();
    }
 else {
      drag(me);
      redraw();
    }
  }
}
