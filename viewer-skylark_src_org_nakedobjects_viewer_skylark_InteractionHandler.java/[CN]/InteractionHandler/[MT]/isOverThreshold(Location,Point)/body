{
  int xDown=downAt2.x;
  int yDown=downAt2.y;
  int x=point.x;
  int y=point.y;
  int threshold=4;
  return x > xDown + threshold || x < xDown - threshold || y > yDown + threshold || y < yDown - threshold;
}
