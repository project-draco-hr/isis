{
  super(configuration);
  this.serverFacade=serverFacade;
  ProxyAuthenticator authenticator=new ProxyAuthenticator(getConfiguration(),serverFacade,encoderDecoder);
  addAuthenticator(authenticator);
}
