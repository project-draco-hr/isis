{
  String searchName=searchName(name);
  reflector();
  for (int i=0; i < fields.length; i++) {
    if (fields[i].getName().equals(searchName)) {
      return fields[i];
    }
  }
  throw new NakedClassException("No field called '" + name + "' in '"+ getSingularName()+ "'");
}
