{
  if (type.equals("R")) {
    return new RootOidDefault(input);
  }
  if (type.equals("A")) {
    return new AggregatedOid(input);
  }
  throw new IllegalArgumentException("Unknown parent Oid type: " + type);
}
