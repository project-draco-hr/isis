{
  final String oidStr=enlistedTarget.toString();
  final EventType eventTypeFor=eventTypeFor(changeKind);
  final Command command=this.command;
  return publishingServiceInternal.newEventMetadata(command,eventTypeFor,currentUser,timestamp,oidStr,enlistedAdapterClass,null,enlistedTarget,null,null,null,null);
}
