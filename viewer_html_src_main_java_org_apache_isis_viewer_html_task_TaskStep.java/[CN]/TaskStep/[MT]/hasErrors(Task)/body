{
  if (task.getError() != null) {
    return true;
  }
  final String[] errors=task.getErrors();
  for (int i=0; i < errors.length; i++) {
    if (errors[i] != null) {
      return true;
    }
  }
  return false;
}
