{
  if (task.getError() != null) {
    return true;
  }
  final String[] errors=task.getErrors();
  for (  final String error : errors) {
    if (error != null) {
      return true;
    }
  }
  return false;
}
