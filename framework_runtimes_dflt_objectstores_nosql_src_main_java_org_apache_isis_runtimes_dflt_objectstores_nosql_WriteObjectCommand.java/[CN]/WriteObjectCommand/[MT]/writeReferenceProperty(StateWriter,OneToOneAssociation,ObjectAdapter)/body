{
  if (otoa.getSpecification().isAggregated()) {
    writeReferencedAsAggregated(writer,otoa,referencedAdapter,keyCreator);
  }
 else {
    writeReference(writer,otoa,referencedAdapter,keyCreator);
  }
}
