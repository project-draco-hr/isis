{
  if (IN_JAVA2) {
    super.printStackTrace(s);
  }
 else {
synchronized (s) {
      s.println(" Exception: " + this);
      super.printStackTrace(s);
      Throwable c=getCause();
      if (c != null) {
        s.println("Caused by: " + this);
        c.printStackTrace(s);
      }
    }
  }
}
