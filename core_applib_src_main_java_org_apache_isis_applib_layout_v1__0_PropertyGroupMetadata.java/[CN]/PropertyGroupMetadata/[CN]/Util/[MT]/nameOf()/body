{
  return new Function<PropertyGroupMetadata,String>(){
    @Nullable @Override public String apply(    @Nullable final PropertyGroupMetadata propertyGroupMetadata){
      return propertyGroupMetadata.getName();
    }
  }
;
}
