{
  int actualSize=getCollectionSize(collectionName);
  if (actualSize == noOfElements) {
    throw new NakedAssertionFailedError("Excepted " + collectionName + " to contain "+ noOfElements+ " instead it contained "+ actualSize);
  }
}
