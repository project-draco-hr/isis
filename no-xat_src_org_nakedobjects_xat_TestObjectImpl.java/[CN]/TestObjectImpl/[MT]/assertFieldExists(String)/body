{
  try {
    ((NakedObject)getForNaked()).getSpecification().getField(fieldName);
  }
 catch (  NakedObjectSpecificationException e) {
    throw new NakedAssertionFailedError("No field called '" + fieldName + "' in "+ getForNaked().getSpecification().getFullName());
  }
}
