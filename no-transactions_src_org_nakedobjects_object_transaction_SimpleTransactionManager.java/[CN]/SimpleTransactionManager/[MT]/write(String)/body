{
  if (file == null) {
    try {
      file=new PrintStream(new FileOutputStream("transaction.log"));
    }
 catch (    IOException e) {
      e.printStackTrace();
    }
  }
  file.println(message);
  file.flush();
}
