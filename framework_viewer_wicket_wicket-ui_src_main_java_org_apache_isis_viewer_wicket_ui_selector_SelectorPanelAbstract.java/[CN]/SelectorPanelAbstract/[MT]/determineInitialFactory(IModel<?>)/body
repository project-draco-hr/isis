{
  return isResolveFacet(model) ? new Predicate<ComponentFactory>(){
    @Override public boolean apply(    @Nullable ComponentFactory input){
      return !(input instanceof CollectionContentsAsUnresolvedFactory);
    }
  }
 : Predicates.<ComponentFactory>alwaysTrue();
}
