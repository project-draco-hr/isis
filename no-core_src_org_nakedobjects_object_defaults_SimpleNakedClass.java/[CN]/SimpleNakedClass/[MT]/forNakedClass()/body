{
  if (nakedClass == null) {
    if (getName().length() == 0) {
      throw new NakedObjectRuntimeException();
    }
    nakedClass=NakedObjectSpecification.getSpecification(getName());
  }
  return nakedClass;
}
