{
  final String className="org.apache.isis.core.progmodel.facets.object.value.ValueFacetFactoryTest$MyValueWithSemanticsProviderSpecifiedUsingConfiguration";
  propertiesConfiguration.add(ValueSemanticsProviderUtil.SEMANTICS_PROVIDER_NAME_KEY_PREFIX + canonical(className) + ValueSemanticsProviderUtil.SEMANTICS_PROVIDER_NAME_KEY_SUFFIX,className);
  facetFactory.process(MyValueWithSemanticsProviderSpecifiedUsingConfiguration.class,methodRemover,facetHolder);
  final ValueFacetAbstract facet=(ValueFacetAbstract)facetHolder.getFacet(ValueFacet.class);
  assertNotNull(facet);
  final ParseableFacet parseableFacet=facetHolder.getFacet(ParseableFacet.class);
  assertNotNull(parseableFacet);
}
