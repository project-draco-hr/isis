{
  ResponseBuilder builder=Response.status(ex.getHttpStatusCode().getJaxrsStatusType()).type(MediaType.APPLICATION_JSON_TYPE).entity(jsonFor(ex));
  String message=ex.getMessage();
  if (message != null) {
    builder.header(RestfulResponse.Header.WARNING.getName(),message);
  }
  return builder.build();
}
