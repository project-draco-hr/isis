{
  final LinkedHashMap<String,FCTab> tabsByName=Maps.newLinkedHashMap();
  visit(new FCPage.VisitorAdapter(){
    @Override public void visit(    final FCTab fcTab){
      tabsByName.put(fcTab.getName(),fcTab);
    }
  }
);
  return tabsByName;
}
