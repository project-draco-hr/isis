{
  t.abort();
  try {
    t.abort();
    fail();
  }
 catch (  TransactionException expected) {
  }
  try {
    t.commit(os,updates);
    fail();
  }
 catch (  TransactionException expected) {
  }
}
