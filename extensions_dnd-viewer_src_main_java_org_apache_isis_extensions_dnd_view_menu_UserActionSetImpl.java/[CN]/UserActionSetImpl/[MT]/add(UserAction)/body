{
  final ObjectActionType section=option.getType();
  if (section == ObjectActionType.USER || (includeExploration && section == ObjectActionType.EXPLORATION) || (includePrototype && section == ObjectActionType.PROTOTYPE) || (includeDebug && section == ObjectActionType.DEBUG)) {
    options.addElement(option);
  }
}
