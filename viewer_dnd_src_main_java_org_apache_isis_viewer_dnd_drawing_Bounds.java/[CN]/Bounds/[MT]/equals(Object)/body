{
  if (obj == this) {
    return true;
  }
  if (obj instanceof Bounds) {
    final Bounds b=(Bounds)obj;
    return b.x == x && b.y == y && b.width == width && b.height == height;
  }
  return false;
}
