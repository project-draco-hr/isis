{
  List<T> result=new ArrayList<T>();
  for (  T element : elements) {
    result.add(element);
  }
  for (  T element : elementsToAppend) {
    if (element != null) {
      result.add(element);
    }
  }
  return result;
}
