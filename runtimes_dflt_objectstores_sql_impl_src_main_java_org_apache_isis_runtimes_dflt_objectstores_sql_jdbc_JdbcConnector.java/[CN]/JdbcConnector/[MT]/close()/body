{
  try {
    if (connection != null) {
      LOG.info("close");
      connection.close();
      connection=null;
    }
  }
 catch (  final SQLException e) {
    throw new SqlObjectStoreException("Failed to close",e);
  }
}
