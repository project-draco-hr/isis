{
  request=client.createRequest(RestfulHttpMethod.GET,"user").withArg(RequestParameter.FOLLOW_LINKS,"links[rel=" + Rel.SELF.getName() + "]");
  restfulResponse=request.executeT();
  repr=restfulResponse.getEntity();
  assertThat(repr.getSelf().getValue(),is(not(nullValue())));
}
