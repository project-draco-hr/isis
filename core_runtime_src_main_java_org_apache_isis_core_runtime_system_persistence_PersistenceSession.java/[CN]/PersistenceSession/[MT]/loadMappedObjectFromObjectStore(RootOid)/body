{
  final ObjectAdapter adapter=getTransactionManager().executeWithinTransaction(new TransactionalClosureWithReturn<ObjectAdapter>(){
    @Override public ObjectAdapter execute(){
      return loadInstanceAndAdapt(oid);
    }
  }
);
  return adapter;
}
