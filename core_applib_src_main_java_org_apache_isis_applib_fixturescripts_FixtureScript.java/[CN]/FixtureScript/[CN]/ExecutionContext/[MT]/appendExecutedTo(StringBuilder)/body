{
  final int max=maxQualifiedNameLength();
  for (  final String qualifiedName : this.fixtureScriptClasses.keySet()) {
    buf.append(pad(qualifiedName,max)).append(": ").append(fixtureScriptClasses.get(qualifiedName).getName()).append("\n");
  }
  return buf;
}
