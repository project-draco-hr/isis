{
  if (command.getTargetAction().equals(Command.TARGET_ACTION_FOR_EDIT)) {
    throw new IllegalArgumentException(String.format("EventMetadata can only be created for commands that represent actions, not property edits; object: %s, member: %s",command.getTarget(),command.getMemberIdentifier()));
  }
  throw new RuntimeException("not yet implemented");
}
