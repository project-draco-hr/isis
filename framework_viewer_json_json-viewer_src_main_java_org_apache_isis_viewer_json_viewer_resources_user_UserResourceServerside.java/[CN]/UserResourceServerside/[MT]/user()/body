{
  RepresentationType user=RepresentationType.USER;
  init(user);
  final RendererFactory factory=rendererFactoryRegistry.find(user);
  final UserReprRenderer renderer=(UserReprRenderer)factory.newRenderer(getResourceContext(),JsonRepresentation.newMap());
  renderer.includesSelf().with(getAuthenticationSession());
  return responseOfOk(Caching.ONE_HOUR,renderer).build();
}
