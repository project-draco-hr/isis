{
  final Method method=processMethodContext.getMethod();
  String value=faIconIfAnyFor(method);
  return value != null ? new CssClassFaFacetOnMemberFromConfiguredRegex(value,processMethodContext.getFacetHolder()) : null;
}
