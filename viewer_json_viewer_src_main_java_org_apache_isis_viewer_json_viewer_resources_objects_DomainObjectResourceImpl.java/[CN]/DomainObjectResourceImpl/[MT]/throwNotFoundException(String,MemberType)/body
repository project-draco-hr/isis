{
  String memberTypeStr=memberType.name().toLowerCase();
  throw new WebApplicationException(responseOfNotFound("No such " + memberTypeStr + "/"+ memberTypeStr+ " not visible: '"+ propertyId+ "'"));
}
