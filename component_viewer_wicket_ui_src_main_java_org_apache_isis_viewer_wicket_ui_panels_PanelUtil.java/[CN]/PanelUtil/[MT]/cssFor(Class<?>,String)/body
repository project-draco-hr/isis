{
  if (cls == null) {
    return null;
  }
  String simpleName=cls.getSimpleName();
  if (Strings.isNullOrEmpty(simpleName)) {
    return null;
  }
  String string;
  if (suffix != null) {
    string=("-" + suffix);
  }
 else {
    string="";
  }
  return simpleName + string + ".css";
}
