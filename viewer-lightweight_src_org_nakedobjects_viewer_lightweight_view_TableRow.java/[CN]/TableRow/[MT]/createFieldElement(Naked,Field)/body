{
  if (naked instanceof NakedValue) {
    return ViewFactory.getViewFactory().createInternalView(naked,field,InternalView.FIELD);
  }
 else {
    if (naked instanceof InternalCollection) {
      return null;
    }
 else     if (naked == null) {
      return (InternalView)emptyCellPrototype.makeView(naked,field);
    }
 else {
      return (InternalView)objectCellPrototype.makeView(naked,field);
    }
  }
}
