{
  if (value == null) {
    return "NULL";
  }
 else {
    String ts=new String(value.asEncodedString());
    if (ts == null || ts.equals("NULL")) {
      return "NULL";
    }
    return "'" + ts + "'";
  }
}
