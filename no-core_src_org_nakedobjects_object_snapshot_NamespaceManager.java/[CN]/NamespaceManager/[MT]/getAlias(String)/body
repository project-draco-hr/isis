{
  String uri=getUri(fullyQualifiedClassName);
  String alias=(String)aliasByUriMap.get(uri);
  if (alias == null) {
    manageNamespaceFor(fullyQualifiedClassName);
    return getAlias(fullyQualifiedClassName);
  }
  return alias;
}
