{
  if (!(model instanceof ValueModel)) {
    return ApplicationAdvice.DOES_NOT_APPLY;
  }
  final ValueModel valueModel=(ValueModel)model;
  final ObjectAdapter specification=valueModel.getObject();
  return appliesIf(specification != null && specification.getSpecification().containsFacet(ValueFacet.class));
}
