{
  final Iterable<Collection> collections=Iterables.transform(Iterables.filter(column.getContent(),is(Collection.class)),cast(Collection.class));
  for (  final Collection collection : collections) {
    visitor.visit(collection);
    visitor.visit(collection.getLayout(),collection);
    traverseActions(collection,visitor);
  }
}
