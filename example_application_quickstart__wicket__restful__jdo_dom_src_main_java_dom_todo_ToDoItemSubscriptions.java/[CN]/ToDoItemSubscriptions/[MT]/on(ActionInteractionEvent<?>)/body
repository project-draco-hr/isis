{
switch (ev.getPhase()) {
case HIDE:
    break;
case DISABLE:
  break;
case VALIDATE:
break;
case EXECUTE:
rejectIfRequired();
recordEvent(ev);
LOG.info(ev.getIdentifier().getMemberName() + ": " + container.titleOf(ev.getSource()));
break;
}
}
