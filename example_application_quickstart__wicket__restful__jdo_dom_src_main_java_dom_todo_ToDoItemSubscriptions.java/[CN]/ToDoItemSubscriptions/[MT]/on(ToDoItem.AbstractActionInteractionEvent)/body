{
switch (ev.getPhase()) {
case HIDE:
    break;
case DISABLE:
  break;
case VALIDATE:
break;
case EXECUTE:
rejectIfRequired();
recordEvent(ev);
LOG.info(ev.getEventDescription() + ": " + container.titleOf(ev.getSource()));
break;
}
}
