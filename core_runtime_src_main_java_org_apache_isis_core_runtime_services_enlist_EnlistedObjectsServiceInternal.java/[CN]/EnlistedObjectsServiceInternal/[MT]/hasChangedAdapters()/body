{
  final Set<Map.Entry<AdapterAndProperty,PreAndPostValues>> changedObjectProperties=getChangedObjectProperties();
  final Set<ObjectAdapter> changedAdapters=Sets.newHashSet(Iterables.filter(Iterables.transform(changedObjectProperties,AdapterAndProperty.Functions.GET_ADAPTER),Predicates.not(IS_COMMAND)));
  return !changedAdapters.isEmpty();
}
