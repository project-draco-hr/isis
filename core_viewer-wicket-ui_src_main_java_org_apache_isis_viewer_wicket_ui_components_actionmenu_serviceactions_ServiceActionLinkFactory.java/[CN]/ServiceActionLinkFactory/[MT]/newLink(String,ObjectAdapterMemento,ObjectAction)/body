{
  ObjectAdapter objectAdapter=adapterMemento.getObjectAdapter(ConcurrencyChecking.NO_CHECK,getPersistenceSession(),getSpecificationLoader());
  final AbstractLink link=newLink(linkId,objectAdapter,action);
  return newLinkAndLabel(objectAdapter,action,link,null);
}
