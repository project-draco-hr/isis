{
  super(id);
  final EntityModel modelWithTabHints=model.cloneWithLayoutMetadata(fcTab);
  getComponentFactoryRegistry().addOrReplaceComponent(this,ID_COLUMN,ComponentType.ENTITY_PROPERTIES,modelWithTabHints);
}
