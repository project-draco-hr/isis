{
  return new Function<ToDoItem.Subcategory,String>(){
    @Override public String apply(    final ToDoItem.Subcategory subcategory){
      return subcategory + ": " + countIn(itemsInCategory,subcategory);
    }
  }
;
}
