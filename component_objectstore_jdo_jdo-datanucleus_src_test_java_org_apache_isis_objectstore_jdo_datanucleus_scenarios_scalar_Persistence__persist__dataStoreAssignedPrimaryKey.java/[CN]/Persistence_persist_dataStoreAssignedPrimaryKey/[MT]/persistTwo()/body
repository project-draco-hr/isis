{
  iswf.beginTran();
  repo.newEntity();
  repo.newEntity();
  iswf.commitTran();
  iswf.bounceSystem();
  iswf.beginTran();
  List<AutoAssignedEntity> list=repo.list();
  assertThat(list.size(),is(2));
  iswf.commitTran();
}
