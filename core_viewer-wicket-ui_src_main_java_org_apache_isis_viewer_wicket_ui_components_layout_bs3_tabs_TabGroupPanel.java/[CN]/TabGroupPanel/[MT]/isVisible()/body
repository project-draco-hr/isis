{
  return FluentIterable.<AbstractTab>from(getTabs()).anyMatch(new Predicate<AbstractTab>(){
    @Override public boolean apply(    final AbstractTab tab){
      return tab.isVisible();
    }
  }
);
}
