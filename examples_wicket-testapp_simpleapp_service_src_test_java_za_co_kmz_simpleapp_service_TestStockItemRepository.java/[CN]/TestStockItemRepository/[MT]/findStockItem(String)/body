{
  StockItem item=null;
  for (int i=0; i < items.size(); i++) {
    StockItem stockItem=items.get(i);
    if (stockItem.title().equalsIgnoreCase(title)) {
      item=stockItem;
    }
  }
  return item;
}
