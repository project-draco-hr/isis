{
  this.server=server;
  if (host == null) {
    listenSocket=new ServerSocket(port);
  }
 else {
    listenSocket=new ServerSocket(port,50,InetAddress.getByName(host));
  }
  listenSocket.setSoTimeout(1000);
  clientThreadGroup=new ThreadGroup("clients");
}
