{
  StringBuilder buf=new StringBuilder();
  buf.append("??? ");
  if (getModel() != null) {
    buildMessageForModel(buf,getModel());
  }
 else {
    buf.append("model is NULL");
  }
  return buf.toString();
}
