{
  getViewManager().showDefaultCursor();
  int x=drag.getTargetLocation().getX();
  int y=drag.getTargetLocation().getY();
  resize=new Size(x,y);
  Logger.getLogger(getClass()).debug("resizing sroll border " + resize);
  invalidateLayout();
}
