{
  adjust(drag);
switch (dragArea) {
case NORTH:
    topHeader.dragTo(drag);
  break;
case WEST:
leftHeader.dragTo(drag);
break;
case CENTER:
wrappedView.dragTo(drag);
break;
case SOUTH:
case EAST:
default :
}
}
