{
  Size size=getSize();
  int width=size.getWidth();
  int height=size.getHeight();
  canvas.drawLine(0,height - SCROLLBAR_WIDTH,width,height - SCROLLBAR_WIDTH,Style.SECONDARY2);
  canvas.drawLine(width - SCROLLBAR_WIDTH,0,width - SCROLLBAR_WIDTH,height,Style.SECONDARY2);
  super.draw(canvas);
}
