{
  int x=mouseLocation.getX();
  int y=mouseLocation.getY();
  if (x >= getSize().getHeight() - SCROLLBAR_WIDTH || y >= getSize().getWidth() - SCROLLBAR_WIDTH) {
    return ViewAreaType.INTERNAL;
  }
  return super.viewAreaType(mouseLocation);
}
