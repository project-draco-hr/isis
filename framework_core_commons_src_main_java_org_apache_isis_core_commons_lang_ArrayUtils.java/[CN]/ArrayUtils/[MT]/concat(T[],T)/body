{
  final List<T> result=new ArrayList<T>();
  for (  final T element : elements) {
    result.add(element);
  }
  for (  final T element : elementsToAppend) {
    if (element != null) {
      result.add(element);
    }
  }
  return result;
}
