{
  ObjectSpecification collectionSpecification=collection.getSpecification();
  return new ArrayProperty().description("List of " + objectTypeFor(collectionSpecification)).items(modelFor(collectionSpecification));
}
