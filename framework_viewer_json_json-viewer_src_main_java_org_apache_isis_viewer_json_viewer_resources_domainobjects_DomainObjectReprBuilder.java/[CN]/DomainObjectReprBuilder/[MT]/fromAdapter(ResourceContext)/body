{
  return new Function<ObjectAdapter,JsonRepresentation>(){
    @Override public JsonRepresentation apply(    ObjectAdapter adapter){
      return newBuilder(resourceContext).with(adapter).build();
    }
  }
;
}
