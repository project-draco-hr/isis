{
  String cls=getClass().getName();
  NakedValue value=null;
  try {
    value=getValue();
  }
 catch (  NullPointerException ignore) {
    ;
  }
  return cls.substring(cls.lastIndexOf('.') + 1) + getId() + " [x="+ getLocation().x+ ",y="+ getLocation().y+ ",object="+ value+ "]";
}
