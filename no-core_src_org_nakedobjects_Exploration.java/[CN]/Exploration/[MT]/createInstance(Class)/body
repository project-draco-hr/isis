{
  NakedClass nc=classManager().getNakedClass(type.getName());
  if (nc == null) {
    return new NakedError("Could not create an object of class " + type);
  }
  NakedObject object=(NakedObject)nc.acquireInstance();
  object.created();
  addInstance(object);
  return object;
}
