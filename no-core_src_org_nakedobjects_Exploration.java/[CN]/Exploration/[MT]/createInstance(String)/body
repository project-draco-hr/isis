{
  NakedClass nc=classManager().getNakedClass(className);
  if (nc == null) {
    return new NakedError("Could not create an object of class " + className);
  }
  NakedObject object=nc.acquireInstance();
  object.created();
  addInstance(object);
  return object;
}
