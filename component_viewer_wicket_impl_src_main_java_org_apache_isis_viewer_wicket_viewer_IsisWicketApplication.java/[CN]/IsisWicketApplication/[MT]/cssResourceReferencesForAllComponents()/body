{
  Collection<ComponentFactory> componentFactories=getComponentFactoryRegistry().listComponentFactories();
  return Sets.newLinkedHashSet(Iterables.concat(Iterables.transform(componentFactories,getCssResourceReferences)));
}
