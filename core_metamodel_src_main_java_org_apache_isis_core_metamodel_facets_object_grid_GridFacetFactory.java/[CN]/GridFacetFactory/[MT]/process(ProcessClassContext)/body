{
  final FacetHolder facetHolder=processClassContext.getFacetHolder();
  final LayoutService layoutService=servicesInjector.lookupService(LayoutService.class);
  FacetUtil.addFacet(GridFacetDefault.create(facetHolder,layoutService,getDeploymentCategory()));
}
