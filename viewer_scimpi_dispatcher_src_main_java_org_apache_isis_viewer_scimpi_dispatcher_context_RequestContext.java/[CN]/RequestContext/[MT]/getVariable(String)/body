{
  for (int i=0; i < SCOPES.length; i++) {
    Map<String,Object> map=variables.get(SCOPES[i]);
    Object object=map.get(name);
    if (object != null) {
      return object;
    }
  }
  return null;
}
