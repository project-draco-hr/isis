{
  if (list.size() == 0) {
    return null;
  }
  final StringBuilder buf=new StringBuilder();
  boolean first=true;
  for (  final String str : list) {
    if (first) {
      first=false;
    }
 else {
      buf.append(delimiter);
    }
    buf.append(str);
  }
  return buf.toString();
}
