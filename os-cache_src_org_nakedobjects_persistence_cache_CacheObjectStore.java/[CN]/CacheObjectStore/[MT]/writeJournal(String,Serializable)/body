{
  LOG.debug("Journal " + action + " - "+ data);
  try {
    journal.writeObject(action);
    journal.writeObject(data);
    journal.flush();
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
