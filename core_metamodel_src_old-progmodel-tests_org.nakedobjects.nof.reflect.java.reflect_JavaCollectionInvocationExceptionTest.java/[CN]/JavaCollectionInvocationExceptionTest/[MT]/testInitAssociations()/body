{
  try {
    TestPojo elementToAdd1=new TestPojo();
    TestPojo elementToAdd2=new TestPojo();
    collectionField.initOneToManyAssociation(testAdapter,new ObjectAdapter[]{system.createPersistentTestObject(elementToAdd1),system.createPersistentTestObject(elementToAdd2)});
    fail();
  }
 catch (  ReflectionException e) {
  }
}
