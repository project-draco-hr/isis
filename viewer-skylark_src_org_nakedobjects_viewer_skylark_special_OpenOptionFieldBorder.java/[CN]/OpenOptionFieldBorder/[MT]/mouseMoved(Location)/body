{
  if (at.getX() >= getSize().getWidth() - right) {
    getViewManager().showArrowCursor();
    if (!over) {
      markDamaged();
    }
    over=true;
  }
 else {
    if (over) {
      markDamaged();
    }
    over=false;
    super.mouseMoved(at);
  }
}
