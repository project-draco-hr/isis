{
  float x=click.getLocation().getX() - 2;
  float boundary=getSize().getWidth() - right;
  if (x >= boundary) {
    if (isAvailable()) {
      open();
    }
    return;
  }
  super.firstClick(click);
}
