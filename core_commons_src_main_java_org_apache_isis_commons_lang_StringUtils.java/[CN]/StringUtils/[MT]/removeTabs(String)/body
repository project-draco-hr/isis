{
  if (text.indexOf('\t') == -1) {
    return text;
  }
  final StringBuffer buf=new StringBuffer();
  for (int i=0; i < text.length(); i++) {
    if (text.charAt(i) != '\t') {
      buf.append(text.charAt(i));
    }
  }
  return buf.toString();
}
