{
  mockConfiguration=mockery.mock(IsisConfiguration.class);
  authenticator=new AuthenticatorAbstractForDfltRuntime(mockConfiguration){
    @Override public boolean canAuthenticate(    final AuthenticationRequest request){
      return false;
    }
    @Override public boolean isValid(    final AuthenticationRequest request){
      return false;
    }
  }
;
}
