{
  return new Predicate<TabGroup>(){
    @Override public boolean apply(    final TabGroup tabGroup){
      return FluentIterable.from(tabGroup.getTabs()).anyMatch(Tab.Predicates.notEmpty());
    }
  }
;
}
