{
  l.add(new DebuggableWithTitle(){
    @Override public void debugData(    DebugString debug){
      debug.append(summary.toString());
    }
    @Override public String debugTitle(){
      return name;
    }
  }
);
}
