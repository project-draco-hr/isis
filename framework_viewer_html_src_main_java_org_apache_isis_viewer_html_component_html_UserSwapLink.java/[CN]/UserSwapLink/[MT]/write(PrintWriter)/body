{
  writer.print("<a class=\"user\" href=\"" + pathTo("setuser") + "?name=");
  writer.print(name);
  writer.print("\" title=\"Change user to " + name);
  writer.print("\">");
  writer.print(name);
  writer.println("</a>");
}
