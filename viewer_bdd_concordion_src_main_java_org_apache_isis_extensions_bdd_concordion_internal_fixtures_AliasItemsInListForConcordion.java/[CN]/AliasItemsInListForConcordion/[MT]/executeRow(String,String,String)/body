{
  if (!getPeer().isList()) {
    return null;
  }
  captureCurrent(aliasAs,title,type);
  try {
    getPeer().findAndAlias();
  }
 catch (  StoryBoundValueException e) {
    return e.getMessage();
  }
  return null;
}
