{
  final Option option=OptionBuilder.withArgName("app manifest").hasArg().withLongOpt(APP_MANIFEST_LONG_OPT).withDescription("fully qualified AppManifest class").create(APP_MANIFEST_OPT);
  options.addOption(option);
}
