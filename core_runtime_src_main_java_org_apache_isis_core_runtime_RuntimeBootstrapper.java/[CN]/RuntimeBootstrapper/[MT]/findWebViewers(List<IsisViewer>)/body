{
  return Lists.newArrayList(Iterables.filter(viewers,new Predicate<IsisViewer>(){
    @Override public boolean apply(    final IsisViewer viewer){
      return viewer.getWebAppSpecification() != null;
    }
  }
));
}
