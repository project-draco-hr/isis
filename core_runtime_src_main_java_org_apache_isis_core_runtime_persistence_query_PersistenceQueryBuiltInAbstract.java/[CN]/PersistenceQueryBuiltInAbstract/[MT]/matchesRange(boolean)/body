{
  if (ifMatches == false) {
    return false;
  }
  if (getCount() == 0 && getStart() == 0) {
    return true;
  }
  if (index++ < start) {
    return false;
  }
  if (countedSoFar++ < count) {
    return true;
  }
  return false;
}
