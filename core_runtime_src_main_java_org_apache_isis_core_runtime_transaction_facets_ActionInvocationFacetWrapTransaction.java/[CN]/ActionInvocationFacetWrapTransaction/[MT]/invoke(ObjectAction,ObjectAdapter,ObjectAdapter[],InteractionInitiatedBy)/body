{
  final ObjectAdapter result=getTransactionManager().executeWithinTransaction(new TransactionalClosureWithReturn<ObjectAdapter>(){
    @Override public ObjectAdapter execute(){
      return underlyingFacet.invoke(owningAction,targetAdapter,argumentAdapters,interactionInitiatedBy);
    }
  }
);
  return result;
}
