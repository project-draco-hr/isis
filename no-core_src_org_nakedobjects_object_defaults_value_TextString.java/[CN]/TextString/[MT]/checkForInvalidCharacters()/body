{
  if (text == null) {
    return;
  }
  for (int i=0; i < text.length(); i++) {
    if (isCharDisallowed(text.charAt(i))) {
      throw new RuntimeException(getClass() + " cannot contain the character code 0x" + Integer.toHexString(text.charAt(i)));
    }
  }
}
