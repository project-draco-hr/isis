{
  if (hasHint()) {
    if (parameters == null) {
      return actionDelegate.getHint(session,object,new NakedObject[0]);
    }
 else {
      return actionDelegate.getHint(session,object,parameters);
    }
  }
 else {
    return new DefaultHint(getLabel());
  }
}
