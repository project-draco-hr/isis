{
  Role user=(Role)object;
  connector.update("update " + table + " set name='"+ user.getName().stringValue()+ "', description='"+ user.getDescription().stringValue()+ "' where id = "+ ((SimpleOid)user.getOid()).getSerialNo());
}
