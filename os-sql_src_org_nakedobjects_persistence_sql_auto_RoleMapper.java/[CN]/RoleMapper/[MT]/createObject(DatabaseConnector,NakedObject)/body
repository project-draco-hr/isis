{
  Role user=(Role)object;
  String id=primaryKey(user.getOid());
  connector.update("Insert into " + table + " ("+ columns+ ") values ('"+ user.getName().stringValue()+ "', '"+ user.getDescription().stringValue()+ "',"+ id+ ")");
}
