{
  DateTime dateTime=sqlDataClass.getDateTime();
  Date date=sqlDataClass.getDate();
  assertTrue("dateTime's value (" + dateTime.dateValue() + ") should be after date's ("+ date+ ")",dateTime.dateValue().after(date.dateValue()));
}
