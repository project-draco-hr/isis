{
  if (false) {
    return container.allMatches(ToDoItem.class,Predicates.and(ToDoItem.Predicates.thoseOwnedBy(currentUserName()),ToDoItem.Predicates.thoseWithSimilarDescription(description)));
  }
 else {
    return container.allMatches(new QueryDefault<ToDoItem>(ToDoItem.class,"todo_autoComplete","ownedBy",currentUserName(),"description",description));
  }
}
