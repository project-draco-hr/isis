{
  NakedObjectSpecification nc=NakedObjectSpecification.getNakedClass(type.getName());
  if (nc == null) {
    return new NakedError("Could not create an object of class " + type);
  }
  return createInstance(nc);
}
