{
  super.assertFieldContains(fieldName,expectedValue);
  if (getField(fieldName) instanceof TestValue) {
    doc("<p>Note that the field <em>" + fieldName + "</em> in the "+ doc.objectString(getForNaked())+ " is now set to '"+ getField(fieldName).getForNaked().titleString()+ "'.");
  }
 else {
    doc("<p>Note that object in the field <em>" + fieldName + "</em> of "+ doc.objectString(getForNaked())+ " now has a title of '"+ getField(fieldName).getForNaked().titleString()+ "'.");
  }
}
