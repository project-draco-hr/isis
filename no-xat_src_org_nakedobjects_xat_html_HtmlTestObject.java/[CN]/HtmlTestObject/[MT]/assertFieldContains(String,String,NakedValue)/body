{
  super.assertFieldContains(message,fieldName,expectedValue);
  doc("<p>Note that the field <em>" + fieldName + "</em> in the "+ doc.objectString(getForObject())+ " is now set to '"+ getField(fieldName).getForObject().titleString()+ "'.");
}
