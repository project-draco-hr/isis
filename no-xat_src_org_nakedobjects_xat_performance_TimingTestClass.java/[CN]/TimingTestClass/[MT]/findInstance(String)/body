{
  Profiler timer=new Profiler("Find instance of " + getTitle());
  Delay.userDelay(4,8);
  timer.start();
  TestObject result=super.findInstance(title);
  timer.stop();
  doc.record(timer);
  return result;
}
