{
  Timer timer=new Timer("Find instance of " + getTitle());
  timer.userDelay(4,8);
  timer.start();
  TestObject result=super.findInstance(title);
  timer.stop();
  doc.record(timer);
  return result;
}
