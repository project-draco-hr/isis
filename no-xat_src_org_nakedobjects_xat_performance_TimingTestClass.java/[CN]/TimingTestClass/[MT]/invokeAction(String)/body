{
  Timer timer=new Timer("c;lass action '" + name + "'");
  timer.userDelay(4,8);
  timer.start();
  TestObject result=super.invokeAction(name);
  timer.stop();
  doc.record(timer);
  return result;
}
