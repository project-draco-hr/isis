{
  Profiler timer=new Profiler("Instances of " + getTitle());
  Delay.userDelay(4,8);
  timer.start();
  TestCollection instance=super.instances();
  timer.stop();
  doc.record(timer);
  return instance;
}
