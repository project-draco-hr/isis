{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof Time)) {
    return false;
  }
  Time object=(Time)obj;
  if (object.isEmpty() && isEmpty()) {
    return true;
  }
  return object.date.equals(date);
}
