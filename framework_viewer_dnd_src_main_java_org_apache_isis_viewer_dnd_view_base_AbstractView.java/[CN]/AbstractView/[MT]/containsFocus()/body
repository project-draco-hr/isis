{
  if (hasFocus()) {
    return true;
  }
  final View[] subviews=getSubviews();
  for (  final View subview : subviews) {
    if (subview != null && subview.containsFocus()) {
      return true;
    }
  }
  return false;
}
