{
  final FCPage metadata=getObjectLayoutMetadata();
  final String xml=jaxbService.toXml(metadata,ImmutableMap.<String,Object>of(Marshaller.JAXB_SCHEMA_LOCATION,TNS + " " + SCHEMA_LOCATION));
  return new Clob(Util.withSuffix(fileName,"xml"),"text/xml",xml);
}
