{
  return new Function<ObjectAdapter,JsonRepresentation>(){
    @Override public JsonRepresentation apply(    ObjectAdapter input){
      return newBuilder(resourceContext,input).build();
    }
  }
;
}
