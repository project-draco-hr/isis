{
  publishActionToPublishingService(objectAction,identifiedHolder,targetAdapter,parameterAdapters,resultAdapter);
  publishActionToPublisherServices(objectAction,identifiedHolder,targetAdapter,parameterAdapters,resultAdapter);
}
