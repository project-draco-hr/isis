{
  final Content content=Toolkit.getContentFactory().createRootContent(object);
  View icon=Toolkit.getViewFactory().createView(new ViewRequirement(content,ViewRequirement.CLOSED | ViewRequirement.ROOT));
  add(iconViews,icon);
  placement.position(this,icon);
  return icon;
}
