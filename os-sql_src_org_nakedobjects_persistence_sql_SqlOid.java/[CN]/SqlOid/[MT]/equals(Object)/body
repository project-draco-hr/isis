{
  if (obj == this) {
    return true;
  }
  if (obj instanceof SqlOid) {
    SqlOid o=((SqlOid)obj);
    return className.equals(o.className) && primaryKey.equals(o.primaryKey);
  }
  return false;
}
