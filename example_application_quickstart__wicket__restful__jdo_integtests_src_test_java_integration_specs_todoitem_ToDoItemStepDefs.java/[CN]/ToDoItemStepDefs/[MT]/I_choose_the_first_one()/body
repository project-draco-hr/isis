{
  @SuppressWarnings("unchecked") List<ToDoItem> notYetComplete=get(null,"notYetComplete",List.class);
  assertThat(notYetComplete.isEmpty(),is(false));
  put("todo","firstToDo",notYetComplete.get(0));
}
