{
  final ObjectSpecification noSpec=adapter.getSpecification();
  return SpecificationFacets.isAlwaysImmutable(noSpec) || (SpecificationFacets.isImmutableOncePersisted(noSpec) && adapter.isPersistent());
}
