{
  LOG.debug("loading user: " + pattern);
  String statement="select " + columns + " from "+ table+ " where name = '"+ ((Role)pattern).getName().stringValue()+ "'";
  return getInstances(connector,statement);
}
