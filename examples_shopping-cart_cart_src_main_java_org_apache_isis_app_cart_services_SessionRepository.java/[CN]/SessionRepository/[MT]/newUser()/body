{
  Session newPersistentInstance=newTransientInstance(Session.class);
  Cart cart=newPersistentInstance(Cart.class);
  Session user=newPersistentInstance;
  user.setCart(cart);
  persist(user);
  return user;
}
