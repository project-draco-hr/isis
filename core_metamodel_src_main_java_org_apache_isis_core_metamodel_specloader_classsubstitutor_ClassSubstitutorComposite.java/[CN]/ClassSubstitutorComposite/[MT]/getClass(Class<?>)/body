{
  Class<?> clsBefore;
  do {
    clsBefore=cls;
    for (    ClassSubstitutor substitutor : substitutors) {
      cls=substitutor.getClass(cls);
      if (cls == null) {
        return null;
      }
    }
  }
 while (clsBefore != cls);
  return cls;
}
