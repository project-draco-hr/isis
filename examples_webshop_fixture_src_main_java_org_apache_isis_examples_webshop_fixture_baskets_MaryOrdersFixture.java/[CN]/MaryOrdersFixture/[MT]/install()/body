{
  final Customer mary=customers.findByEmail("mary@smith.com");
  Basket basket=baskets.basketFor(mary);
  addItem(basket,5,2);
  addItem(basket,6,1);
  addItem(basket,11,1);
  @SuppressWarnings("unused") final Order order=basket.checkout();
}
