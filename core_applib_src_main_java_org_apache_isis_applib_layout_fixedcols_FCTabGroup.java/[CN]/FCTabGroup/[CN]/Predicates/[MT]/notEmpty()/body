{
  return new Predicate<FCTabGroup>(){
    @Override public boolean apply(    final FCTabGroup tabGroup){
      return FluentIterable.from(tabGroup.getTabs()).anyMatch(FCTab.Predicates.notEmpty());
    }
  }
;
}
