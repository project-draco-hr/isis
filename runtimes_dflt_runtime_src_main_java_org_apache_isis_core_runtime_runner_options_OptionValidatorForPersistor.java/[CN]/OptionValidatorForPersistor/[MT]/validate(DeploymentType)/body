{
  String objectPersistorName=optionHandlerPersistor.getPersistorName();
  boolean fail=(!StringUtils.isNullOrEmpty(objectPersistorName)) && !deploymentType.canSpecifyObjectStore();
  String failMsg=String.format("Error: cannot specify an object store (persistor) for deployment type %s\n",deploymentType.name().toLowerCase());
  return Maybe.setIf(fail,failMsg);
}
