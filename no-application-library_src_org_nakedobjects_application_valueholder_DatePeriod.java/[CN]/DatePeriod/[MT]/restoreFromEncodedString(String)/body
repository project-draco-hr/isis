{
  if (data == null || data.equals("NULL")) {
    clear();
  }
 else {
    int split=data.indexOf('~');
    getStart().restoreFromEncodedString(data.substring(0,split));
    getEnd().restoreFromEncodedString(data.substring(split + 1));
  }
}
