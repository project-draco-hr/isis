{
  final Consent visible=nakedObjectMember.isVisible(getAuthenticationSession(),getOnAdapter());
  if (visible.isVetoed()) {
    throw StoryBoundValueException.current(onMemberBinding,"(not visible)");
  }
}
