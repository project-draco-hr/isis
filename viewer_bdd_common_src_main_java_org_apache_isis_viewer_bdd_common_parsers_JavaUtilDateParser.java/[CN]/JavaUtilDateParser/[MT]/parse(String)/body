{
  try {
    final SimpleDateFormat parser=new SimpleDateFormat("d MMM yyyy hh:mm");
    final Date parsed=parser.parse(dateStr);
    return parsed;
  }
 catch (  final ParseException e) {
    return null;
  }
}
