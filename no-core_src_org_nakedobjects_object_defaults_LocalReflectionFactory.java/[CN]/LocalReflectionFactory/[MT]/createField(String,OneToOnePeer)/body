{
  OneToOnePeer oneToOneDelegate=new OneToOneTransaction(peer);
  oneToOneDelegate=new OneToOneHelp(oneToOneDelegate,helpManager);
  OneToOneAssociation association=new OneToOneAssociation(className,oneToOneDelegate.getName(),oneToOneDelegate.getType(),oneToOneDelegate);
  return association;
}
