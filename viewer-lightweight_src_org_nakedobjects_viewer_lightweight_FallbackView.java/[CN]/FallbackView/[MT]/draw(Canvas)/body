{
  super.draw(canvas);
  Bounds r=getBounds();
  canvas.drawRectangle(0,0,r.width - 1,r.height - 1,Style.INVALID);
  if (isIdentified()) {
    for (int y=2; y < r.height - 2; y+=2) {
      int x=r.width;
      canvas.drawLine(x - VIEW_CONTROL_WIDTH + 1,y,x - 3,y,Style.ACTIVE);
    }
  }
}
