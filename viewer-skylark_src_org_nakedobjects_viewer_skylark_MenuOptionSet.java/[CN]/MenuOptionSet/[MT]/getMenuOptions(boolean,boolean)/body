{
  Vector v=new Vector();
  createMenu(v,VIEW);
  createMenu(v,OBJECT);
  createMenu(v,CLASS);
  if (includeExploration) {
    createMenu(v,EXPLORATION);
  }
  if (includeDebug) {
    createMenu(v,DEBUG);
  }
  if (v.size() > 0) {
    v.removeElementAt(v.size() - 1);
  }
  return v;
}
