{
  return new Predicate<ToDoItem>(){
    @Override public boolean apply(    final ToDoItem t){
      return Objects.equal(toDoItem.getCategory(),t.getCategory()) && Objects.equal(toDoItem.getOwnedBy(),t.getOwnedBy()) && t != toDoItem;
    }
  }
;
}
