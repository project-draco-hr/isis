{
  return new Predicate<TabGroupMetadata>(){
    @Override public boolean apply(    final TabGroupMetadata tabGroup){
      return FluentIterable.from(tabGroup.getTabs()).anyMatch(TabMetadata.Predicates.notEmpty());
    }
  }
;
}
