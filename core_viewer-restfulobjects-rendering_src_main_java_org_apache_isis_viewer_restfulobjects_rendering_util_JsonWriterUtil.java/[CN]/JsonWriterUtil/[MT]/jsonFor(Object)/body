{
  final JsonMapper.PrettyPrinting prettyPrinting=inferPrettyPrinting(IsisContext.getSessionFactory().getDeploymentCategory());
  try {
    return JsonMapper.instance(prettyPrinting).write(object);
  }
 catch (  final IOException e) {
    throw new RuntimeException(e);
  }
}
