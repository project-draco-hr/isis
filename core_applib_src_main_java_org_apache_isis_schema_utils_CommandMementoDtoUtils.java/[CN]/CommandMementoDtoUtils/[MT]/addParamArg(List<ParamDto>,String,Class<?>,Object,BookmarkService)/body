{
  boolean isValueType=addParamArgValue(params,parameterName,parameterType,arg);
  if (!isValueType) {
    addParamArgReference(params,parameterName,arg instanceof Bookmark ? (Bookmark)arg : bookmarkService.bookmarkFor(arg));
  }
}
