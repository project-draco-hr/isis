{
  return Filters.and(ObjectAction.Filters.VISIBLE_AT_LEAST_SOMETIMES,new Filter<ObjectAction>(){
    @Override public boolean accept(    ObjectAction t){
      return !excludedActionIds.contains(t.getId());
    }
  }
);
}
