{
  return Filters.and(ObjectActionFilters.WHEN_VISIBLE_IRRESPECTIVE_OF_WHERE,new Filter<ObjectAction>(){
    @Override public boolean accept(    ObjectAction t){
      return !excludedActionIds.contains(t.getId());
    }
  }
);
}
