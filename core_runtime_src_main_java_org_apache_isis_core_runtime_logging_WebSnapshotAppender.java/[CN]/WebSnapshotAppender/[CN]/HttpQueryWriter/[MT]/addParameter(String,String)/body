{
  if (name == null || value == null) {
    return;
  }
  if (parameter > 1) {
    write("&");
  }
  parameter++;
  write(URLEncoder.encode(name,"UTF-8"));
  write("=");
  write(URLEncoder.encode(value,"UTF-8"));
}
