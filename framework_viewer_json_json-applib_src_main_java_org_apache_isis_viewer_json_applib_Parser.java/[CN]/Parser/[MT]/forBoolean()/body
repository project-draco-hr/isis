{
  return new Parser<Boolean>(){
    @Override public Boolean valueOf(    String str){
      return str.equals("yes") ? Boolean.TRUE : Boolean.FALSE;
    }
    @Override public String asString(    Boolean t){
      return t ? "yes" : "no";
    }
  }
;
}
