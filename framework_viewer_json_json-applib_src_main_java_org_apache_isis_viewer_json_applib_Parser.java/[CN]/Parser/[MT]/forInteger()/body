{
  return new Parser<Integer>(){
    @Override public Integer valueOf(    String str){
      if (str == null) {
        return null;
      }
      return Integer.valueOf(str);
    }
    @Override public String asString(    Integer t){
      return t.toString();
    }
  }
;
}
