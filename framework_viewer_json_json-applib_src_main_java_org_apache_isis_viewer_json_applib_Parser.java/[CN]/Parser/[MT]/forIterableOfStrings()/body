{
  return new Parser<Iterable<String>>(){
    @Override public Iterable<String> valueOf(    String str){
      return Splitter.on(",").split(str);
    }
    @Override public String asString(    Iterable<String> strings){
      return Joiner.on(",").join(strings);
    }
  }
;
}
