{
  return new Parser<MediaType>(){
    @Override public MediaType valueOf(    String str){
      if (str == null) {
        return null;
      }
      return MediaType.valueOf(str);
    }
    @Override public String asString(    MediaType t){
      return t.toString();
    }
  }
;
}
