{
  String pluralName;
  if (name.endsWith("y")) {
    pluralName=name.substring(0,name.length() - 1) + "ies";
  }
 else   if (name.endsWith("s")) {
    pluralName=name + "es";
  }
 else {
    pluralName=name + 's';
  }
  return pluralName;
}
