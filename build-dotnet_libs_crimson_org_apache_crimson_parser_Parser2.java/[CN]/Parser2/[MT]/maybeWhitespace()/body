{
  if (!(inExternalPE && doLexicalPE))   return in.maybeWhitespace();
  char c=getc();
  boolean saw=false;
  while (c == ' ' || c == '\t' || c == '\n' || c == '\r') {
    saw=true;
    if (in.isEOF() && !in.isInternal())     return saw;
    c=getc();
  }
  ungetc();
  return saw;
}
