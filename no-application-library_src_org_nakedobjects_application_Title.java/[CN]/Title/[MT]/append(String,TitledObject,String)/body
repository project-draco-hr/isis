{
  if (string.length() > 0 && (object != null && titleString(object).length() > 0) || (defaultValue != null && defaultValue.length() > 0)) {
    concat(joiner);
    appendSpace();
  }
  concat(object,defaultValue);
  return this;
}
