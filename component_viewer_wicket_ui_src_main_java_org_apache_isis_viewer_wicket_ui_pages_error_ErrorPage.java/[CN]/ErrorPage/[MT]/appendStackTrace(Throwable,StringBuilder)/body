{
  for (  StackTraceElement el : ex.getStackTrace()) {
    buf.append(el.getClassName()).append(el.getMethodName()).append("(").append(el.getFileName()).append(":").append(el.getLineNumber()).append(")\n");
  }
}
