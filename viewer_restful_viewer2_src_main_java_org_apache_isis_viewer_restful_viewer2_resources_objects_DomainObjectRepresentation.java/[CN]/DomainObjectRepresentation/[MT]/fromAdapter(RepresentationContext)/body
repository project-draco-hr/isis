{
  return new Function<ObjectAdapter,DomainObjectRepresentation>(){
    @Override public DomainObjectRepresentation apply(    ObjectAdapter input){
      return newBuilder(representationContext,input).build();
    }
  }
;
}
