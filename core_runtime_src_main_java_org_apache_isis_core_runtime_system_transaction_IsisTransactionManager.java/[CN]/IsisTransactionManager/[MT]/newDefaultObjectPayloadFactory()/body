{
  return new PublishedObject.PayloadFactory(){
    @Override public EventPayload payloadFor(    final Object changedObject){
      return new EventPayloadForChangedObject(changedObject).with(objectStringifier());
    }
  }
;
}
