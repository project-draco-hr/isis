{
  return new PublishedObject.EventCanonicalizer(){
    @Override public CanonicalEvent canonicalizeObject(    final Object changedObject){
      return new CanonicalEvent.Default("CHANGED_OBJECT\n    " + oidStrFor(changedObject));
    }
  }
;
}
