{
  if (addDelay) {
    int delay=min * 1000 + (int)((max - min) * Math.random() * 1000);
    try {
      Thread.sleep(delay);
    }
 catch (    InterruptedException e) {
      e.printStackTrace();
    }
  }
}
