{
  ObjectMember member=specification.getObjectAction(ActionType.USER,id.getMemberName(),getMemberParameterSpecifications(id));
  if (member == null) {
    throw new IsisException("No user action found for id " + id);
  }
  return member;
}
