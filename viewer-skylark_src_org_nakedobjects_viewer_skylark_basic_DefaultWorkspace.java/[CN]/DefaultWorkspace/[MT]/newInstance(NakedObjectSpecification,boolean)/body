{
  NakedObject object=getObject().getContext().getObjectManager().createInstance(cls);
  return openAView ? ViewFactory.getViewFactory().createOpenRootView(object) : ViewFactory.getViewFactory().createIconizedRootView(object);
}
