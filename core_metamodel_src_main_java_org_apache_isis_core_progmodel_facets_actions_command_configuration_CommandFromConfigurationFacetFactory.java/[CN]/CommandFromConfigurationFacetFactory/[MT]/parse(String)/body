{
  if ("ignoreQueryOnly".equalsIgnoreCase(value) || "ignoreSafe".equalsIgnoreCase(value)) {
    return IGNORE_SAFE;
  }
 else   if ("all".equals(value)) {
    return ALL;
  }
 else {
    return NONE;
  }
}
