{
  context.checking(new Expectations(){
{
      one(mockFacetedMethod).getIdentifier();
      will(returnValue(Identifier.actionIdentifier("Customer","reduceheadcount")));
    }
  }
);
  action=new ObjectActionImpl(mockFacetedMethod,new ObjectMemberDependencies(mockSpecificationLookup,mockAdapterManager,mockQuerySubmitter,mockServicesProvider));
}
