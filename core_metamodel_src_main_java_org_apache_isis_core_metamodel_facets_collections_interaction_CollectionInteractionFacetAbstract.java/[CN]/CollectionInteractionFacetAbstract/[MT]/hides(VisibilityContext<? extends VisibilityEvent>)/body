{
  if (!interactionHelper.hasEventBusService()) {
    return null;
  }
  currentInteraction.set(null);
  final CollectionInteractionEvent<?,?> event=interactionHelper.postEventForCollection(eventType(),null,AbstractInteractionEvent.Phase.HIDE,getIdentified(),ic.getTarget(),CollectionInteractionEvent.Of.ACCESS,null);
  if (event != null && event.isHidden()) {
    return "Hidden by subscriber";
  }
  return null;
}
