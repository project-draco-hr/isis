{
  StringBuffer s=new StringBuffer();
  s.append("NakedClass");
  s.append(" [");
  s.append(className);
  s.append("]");
  s.append("  " + Long.toHexString(super.hashCode()).toUpperCase());
  return s.toString();
}
