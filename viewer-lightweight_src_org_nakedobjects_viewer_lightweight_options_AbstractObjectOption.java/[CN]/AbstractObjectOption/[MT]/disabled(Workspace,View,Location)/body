{
  if (view instanceof ObjectView) {
    return disabled(workspace,(ObjectView)view,at);
  }
 else {
    throw new NakedObjectRuntimeException("Menu option " + this + " is incompatible with the view "+ view);
  }
}
