{
  FacetUtil.addFacet(new HiddenObjectFacetViaHiddenMethod(facetMethod,HiddenObjectFacetViaHiddenMethod.class,facetHolder,false));
  for (  Method method : methods) {
    if (!anIsisMethod(method.getName())) {
    }
  }
}
