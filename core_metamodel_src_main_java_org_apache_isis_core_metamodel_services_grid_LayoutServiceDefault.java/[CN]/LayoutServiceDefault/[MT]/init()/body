{
  pageImplementations=FluentIterable.from(gridImplementationServices).transform(new Function<GridImplementationService,Class<? extends Grid>>(){
    @Override public Class<? extends Grid> apply(    final GridImplementationService gridImplementationService){
      return gridImplementationService.gridImplementation();
    }
  }
).toList();
}
