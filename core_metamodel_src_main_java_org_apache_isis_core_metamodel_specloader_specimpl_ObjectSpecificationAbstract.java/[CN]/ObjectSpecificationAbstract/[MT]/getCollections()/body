{
  final List<OneToManyAssociation> list=Lists.newArrayList();
  @SuppressWarnings("rawtypes") final List associationList=getAssociations(ObjectAssociationFilters.COLLECTIONS);
  list.addAll(associationList);
  return list;
}
