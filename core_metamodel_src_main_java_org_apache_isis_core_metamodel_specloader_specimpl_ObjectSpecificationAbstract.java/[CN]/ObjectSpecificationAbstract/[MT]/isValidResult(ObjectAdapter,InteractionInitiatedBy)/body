{
  final ObjectValidityContext validityContext=createValidityInteractionContext(deploymentCategory,getAuthenticationSession(),interactionInitiatedBy,targetObjectAdapter);
  return InteractionUtils.isValidResult(this,validityContext);
}
