{
  Thread t=new Thread("background task"){
    public void run(){
      view.getState().setActive();
      repaint(view);
      task.execute();
      view.getState().setInactive();
      view.markDamaged();
      repaint(view);
    }
  }
;
  t.start();
}
