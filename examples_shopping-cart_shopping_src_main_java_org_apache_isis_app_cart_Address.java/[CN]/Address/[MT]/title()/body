{
  if (address == null) {
    return "";
  }
 else {
    int breakAt=address.indexOf('\n');
    if (breakAt == -1) {
      return address;
    }
 else {
      return address.substring(0,breakAt);
    }
  }
}
