{
  final IsisSystemForTest isisSystem=new IsisSystemForTest(configuration,programmingModel,metaModelValidator,persistenceMechanismInstaller,authenticationRequest,services,fixtures,listeners);
  if (level != null) {
    isisSystem.setLevel(level);
  }
  return isisSystem;
}
