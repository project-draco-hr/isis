{
  if (firstTime) {
    for (    Listener listener : listeners) {
      listener.init(configurationOverride);
    }
  }
  for (  Listener listener : listeners) {
    listener.preSetupSystem(firstTime);
  }
}
