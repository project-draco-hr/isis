{
  if (!isValidListAlias()) {
    return false;
  }
  ObjectAdapter adapter=getAliasRegistry().getAliased(alias);
  if (adapter == null) {
    return false;
  }
  return collectionAdapters().contains(adapter);
}
