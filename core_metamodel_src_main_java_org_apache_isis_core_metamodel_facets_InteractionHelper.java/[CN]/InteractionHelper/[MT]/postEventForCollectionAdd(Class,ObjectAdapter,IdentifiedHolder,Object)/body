{
  if (!hasEventBusService()) {
    return null;
  }
  try {
    final Object source=AdapterUtils.unwrap(targetAdapter);
    final CollectionInteractionEvent<?,?> event=InteractionWithCollectionAddFacet.Util.newEvent(eventType,source,identified.getIdentifier(),addedReference);
    event.setMode(mode);
    getEventBusService().post(event);
    return event;
  }
 catch (  Exception e) {
    throw new FatalException(e);
  }
}
