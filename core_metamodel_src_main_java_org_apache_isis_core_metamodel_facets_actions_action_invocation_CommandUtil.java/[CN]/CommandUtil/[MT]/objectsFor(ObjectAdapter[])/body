{
  List<ObjectAdapter> argList=Arrays.asList(arguments);
  Iterable<Object> adapterList=Iterables.transform(argList,ObjectAdapter.Functions.getObject());
  return Lists.newArrayList(adapterList).toArray(new Object[]{});
}
