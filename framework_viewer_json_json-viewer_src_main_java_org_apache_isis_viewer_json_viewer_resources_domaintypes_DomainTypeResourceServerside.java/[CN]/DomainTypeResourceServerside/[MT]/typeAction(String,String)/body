{
  init();
  JsonRepresentation representation=JsonRepresentation.newMap();
  representation.mapPut("self",LinkBuilder.newBuilder(getResourceContext(),"self",RepresentationType.TYPE_ACTION,"domainTypes/%s/actions/%s",domainType,actionId).build());
  return responseOfOk(RepresentationType.TYPE_ACTION,Caching.ONE_DAY,representation).build();
}
