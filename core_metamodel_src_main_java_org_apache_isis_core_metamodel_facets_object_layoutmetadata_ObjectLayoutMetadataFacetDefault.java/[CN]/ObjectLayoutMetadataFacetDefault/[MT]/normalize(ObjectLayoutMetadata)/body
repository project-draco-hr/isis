{
  if (metadata == null) {
    return null;
  }
  if (metadata.isNormalized()) {
    return metadata;
  }
  doNormalize(metadata,getSpecification());
  metadata.setNormalized(true);
  return metadata;
}
