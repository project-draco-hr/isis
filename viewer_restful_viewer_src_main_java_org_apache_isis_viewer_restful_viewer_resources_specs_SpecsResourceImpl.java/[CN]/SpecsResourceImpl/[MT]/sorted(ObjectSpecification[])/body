{
  Arrays.sort(noSpecs,new Comparator<ObjectSpecification>(){
    @Override public int compare(    ObjectSpecification o1,    ObjectSpecification o2){
      return o1.getFullIdentifier().compareTo(o2.getFullIdentifier());
    }
  }
);
  return noSpecs;
}
