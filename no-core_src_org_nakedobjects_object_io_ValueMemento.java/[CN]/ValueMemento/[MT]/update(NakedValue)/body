{
  if (!value.getSpecification().getFullName().equals(type)) {
    throw new IllegalArgumentException("The value of type " + value.getSpecification().getFullName() + " can not be updated by an object of a different type: "+ type);
  }
  value.restoreString(encodedValue);
}
