{
  final InteractionDto interactionDto=newInteractionDto(transactionId);
  final InteractionExecutionDto executionDto=newPropertyEdit(sequence,targetBookmark,targetTitle,propertyIdentifier,newValueDto,user,transactionId);
  addExecution(interactionDto,executionDto);
  return interactionDto;
}
