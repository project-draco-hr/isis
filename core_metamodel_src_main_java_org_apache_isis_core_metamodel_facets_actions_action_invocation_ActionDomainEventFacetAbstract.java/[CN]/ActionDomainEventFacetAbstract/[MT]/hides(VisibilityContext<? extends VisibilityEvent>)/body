{
  if (!interactionHelper.hasEventBusService()) {
    return null;
  }
  currentInteraction.set(null);
  final ActionDomainEvent<?> event=interactionHelper.postEventForAction(eventType(),null,null,AbstractDomainEvent.Phase.HIDE,getIdentified(),ic.getTarget(),null);
  if (event != null && event.isHidden()) {
    return "Hidden by subscriber";
  }
  return null;
}
