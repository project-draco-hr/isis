{
  final PublishedObject.PayloadFactory payloadFactoryToUse=annotatedPayloadFactoryIfAny != null ? annotatedPayloadFactoryIfAny : defaultObjectPayloadFactory;
  final EventPayload payload=payloadFactoryToUse.payloadFor(ObjectAdapterUtils.unwrapObject(changedAdapter));
  publishingService.publish(metadata,payload);
}
