{
  Size size=super.getRequiredSize();
  int width=getLeft() + HPADDING + TITLE_STYLE.stringWidth(title())+ HPADDING+ controls.length * (WindowControl.WIDTH + HPADDING) + HPADDING + getRight();
  size.ensureWidth(width);
  return size;
}
