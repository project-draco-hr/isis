{
  SqlDataClass sqlDataClass=SqlIntegrationTestSingleton.getPerson();
  Logger.getRootLogger().log(Level.INFO,sqlDataClass.getSqlDate());
  assertTrue("SQL date " + sqlDate.toString() + " is not equal to "+ sqlDataClass.getSqlDate().toString(),sqlDate.compareTo(sqlDataClass.getSqlDate()) == 0);
}
