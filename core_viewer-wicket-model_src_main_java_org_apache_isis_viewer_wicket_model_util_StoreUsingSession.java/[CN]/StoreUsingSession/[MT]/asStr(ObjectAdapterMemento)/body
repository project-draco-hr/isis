{
  final ObjectAdapter objectAdapter=objectAdapterMemento.getObjectAdapter(AdapterManager.ConcurrencyChecking.NO_CHECK);
  final RootOid oid=(RootOid)objectAdapter.getOid();
  return IsisContext.getOidMarshaller().marshalNoVersion(oid);
}
