{
  return new Function<ObjectAdapter,DomainObjectRep>(){
    @Override public DomainObjectRep apply(    ObjectAdapter input){
      return newBuilder(representationContext,input).build();
    }
  }
;
}
