{
  this.model=model;
  this.componentFactories=locateComponentFactories(componentFactoryRegistry);
  this.selectedItemSessionAttribute=selectedItemSessionAttribute != null ? selectedItemSessionAttribute : ScopedSessionAttribute.<String>noop();
  model.getEntityModel().putSessionAttribute(selectedItemSessionAttribute);
}
