{
  cachedHashCode=17;
  cachedHashCode=37 * cachedHashCode + className.hashCode();
  cachedHashCode=37 * cachedHashCode + primaryKey.hashCode();
  cachedToString=(isTransient() ? "T" : "") + "HOID#" + primaryKey.toString()+ "/"+ className+ (hibernateId == null ? "" : "(" + hibernateId + ")")+ (previous == null ? "" : "+");
}
