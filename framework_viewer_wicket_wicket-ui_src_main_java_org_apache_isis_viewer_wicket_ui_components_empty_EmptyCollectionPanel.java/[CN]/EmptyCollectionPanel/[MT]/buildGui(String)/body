{
  final ActionModel model=getModel();
  addOrReplace(new Button("ok",Model.of("OK")){
    private static final long serialVersionUID=1L;
    @Override public void onSubmit(){
      model.getNoResultsHandler().onNoResults(this);
    }
  }
);
}
