{
  Prototypes prototypes=(NakedCollection.class.isAssignableFrom(forType)) ? subLevelCollectionPrototypes : subLevelObjectPrototypes;
  getPrototypes(prototypes,forType).addElement(view);
  LOG.info("Added second level view " + view.getName() + " to "+ forType+ " prototypes");
}
