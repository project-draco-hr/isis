{
  View prototype=defaultPrototypeFor(object,topLevelCollectionPrototypes,topLevelObjectPrototypes);
  View window=create(prototype,object,null);
  LOG.debug(window + " created for " + object);
  return (RootView)window;
}
