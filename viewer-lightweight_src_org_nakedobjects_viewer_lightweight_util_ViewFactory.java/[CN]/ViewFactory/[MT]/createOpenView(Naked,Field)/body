{
  View prototype=defaultPrototypeFor(object,subLevelCollectionPrototypes,subLevelObjectPrototypes);
  View view=create(prototype,object,field);
  LOG.debug(view + " created for " + object);
  return (InternalView)view;
}
