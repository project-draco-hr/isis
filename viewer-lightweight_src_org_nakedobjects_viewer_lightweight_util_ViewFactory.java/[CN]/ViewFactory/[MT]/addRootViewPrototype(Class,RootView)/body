{
  Prototypes prototypes=(NakedCollection.class.isAssignableFrom(forType)) ? topLevelCollectionPrototypes : topLevelObjectPrototypes;
  getPrototypes(prototypes,forType).addElement(view);
  LOG.info("Added top level view " + view.getName() + " to "+ forType+ " prototypes");
}
