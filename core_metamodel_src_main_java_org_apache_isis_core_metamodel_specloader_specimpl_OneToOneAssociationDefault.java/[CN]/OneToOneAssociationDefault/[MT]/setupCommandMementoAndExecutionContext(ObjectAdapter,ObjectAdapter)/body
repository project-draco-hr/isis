{
  final CommandDtoService commandDtoService=getCommandDtoService();
  final CommandDto dto=commandDtoService.asCommandDto(Collections.singletonList(targetAdapter),this,valueAdapterOrNull);
  setupCommandDtoAndExecutionContext(dto);
}
