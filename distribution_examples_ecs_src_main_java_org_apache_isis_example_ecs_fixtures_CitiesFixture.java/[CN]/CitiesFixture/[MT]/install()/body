{
  String[] cityNames={"New York","Boston","Washington","Chicago","Tampa","Seattle","Atlanta"};
  for (int i=0; i < cityNames.length; i++) {
    City city=(City)newTransientInstance(City.class);
    city.setName(cityNames[i]);
    persist(city);
  }
}
