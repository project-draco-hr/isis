{
  history.remove(entry);
  history.add(entry);
  LOG.debug("added to history: " + entry);
  if (history.size() > MAX) {
    LOG.debug("purging from history: " + history.get(0));
    history.remove(0);
  }
}
