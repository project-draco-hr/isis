{
  MockObjectPersistor objectManager=new MockObjectPersistor();
  NakedObjects.reset();
  new NakedObjectsClient().setObjectPersistor(objectManager);
  assertEquals(objectManager,NakedObjects.getObjectPersistor());
}
