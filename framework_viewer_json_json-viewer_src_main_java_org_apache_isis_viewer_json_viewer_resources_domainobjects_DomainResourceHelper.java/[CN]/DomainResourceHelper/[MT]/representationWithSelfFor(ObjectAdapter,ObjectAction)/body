{
  JsonRepresentation representation=JsonRepresentation.newMap();
  String oid=OidUtils.getOidStr(resourceContext,objectAdapter);
  final JsonRepresentation repBuilder=LinkReprBuilder.newBuilder(resourceContext,"self",MediaType.APPLICATION_JSON_TYPE,"objects/%s/actions/%s/invoke",oid,action.getId()).render();
  representation.mapPut("self",repBuilder);
  return representation;
}
