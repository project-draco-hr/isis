{
  final String overridePattern=OVERRIDE_PATTERN.get();
  if (overridePattern == null || titleStringFormatNameOrPattern.equals(overridePattern)) {
    return;
  }
  updateTitleStringFormatter(overridePattern);
}
