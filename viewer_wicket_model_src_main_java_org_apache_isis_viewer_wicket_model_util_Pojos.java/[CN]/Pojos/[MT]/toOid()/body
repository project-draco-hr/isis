{
  return new Function<Object,Oid>(){
    @Override public Oid apply(    final Object pojo){
      final ObjectAdapter adapter=getAdapterManager().adapterFor(pojo);
      return adapter.getOid();
    }
  }
;
}
