{
  profiler.start();
  for (int i=0; i < 20000; i++) {
    setAssociation();
    if (i > 0 && i % 100 == 0) {
      profiler.stop();
      System.out.println(i + "\t" + profiler.timeLog()+ "\t"+ Profiler.memoryLog());
      profiler.reset();
    }
  }
}
