{
  return allMatches(Claim.class,new Filter<Claim>(){
    @Override public boolean accept(    Claim pojo){
      return pojo.getClaimant() == claimant && pojo.getDate() != null && pojo.getDate().isGreaterThan(since);
    }
  }
);
}
