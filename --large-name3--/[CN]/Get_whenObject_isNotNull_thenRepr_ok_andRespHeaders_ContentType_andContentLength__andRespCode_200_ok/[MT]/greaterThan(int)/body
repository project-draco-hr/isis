{
  return new TypeSafeMatcher<Integer>(){
    @Override public void describeTo(    Description description){
      description.appendText("greater than " + i);
    }
    @Override protected boolean matchesSafely(    Integer item){
      return item > i;
    }
  }
;
}
