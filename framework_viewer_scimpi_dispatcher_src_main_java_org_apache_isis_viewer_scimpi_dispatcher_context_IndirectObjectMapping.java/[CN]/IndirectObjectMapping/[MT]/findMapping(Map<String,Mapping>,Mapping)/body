{
  final Iterator<String> ids=map.keySet().iterator();
  while (ids.hasNext()) {
    final String key=ids.next();
    if (map.get(key).equals(mapping)) {
      return key;
    }
  }
  return null;
}
