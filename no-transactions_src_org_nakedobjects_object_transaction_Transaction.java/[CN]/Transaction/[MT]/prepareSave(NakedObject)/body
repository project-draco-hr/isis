{
  if (inCommit) {
    throw new NakedObjectRuntimeException();
  }
  if (toSave.contains(ob) || toCreate.contains(ob)) {
    return;
  }
  if (toDestroy.contains(ob)) {
    return;
  }
  toSave.addElement(ob);
}
