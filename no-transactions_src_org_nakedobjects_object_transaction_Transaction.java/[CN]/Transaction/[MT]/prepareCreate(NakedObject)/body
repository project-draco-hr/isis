{
  if (inCommit) {
    throw new NakedObjectRuntimeException();
  }
  if (toCreate.contains(object)) {
    throw new NakedObjectRuntimeException();
  }
  loaded(object);
  toCreate.addElement(object);
}
