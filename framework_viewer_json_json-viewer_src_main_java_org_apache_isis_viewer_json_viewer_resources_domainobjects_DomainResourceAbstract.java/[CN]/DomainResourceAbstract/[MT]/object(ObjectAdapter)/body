{
  ResourceContext resourceContext=getResourceContext();
  final AbstractReprBuilder<?> repBuilder=DomainObjectReprBuilder.newBuilder(resourceContext).withAdapter(objectAdapter);
  ResponseBuilder respBuilder=responseOfOk(RepresentationType.DOMAIN_OBJECT,Caching.NONE,repBuilder);
  Version version=objectAdapter.getVersion();
  if (version != null && version.getTime() != null) {
    respBuilder.lastModified(version.getTime());
  }
  return respBuilder.build();
}
