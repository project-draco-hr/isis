{
  final ObjectAction action=getObjectActionThatIsVisibleAndUsable(serviceAdapter,actionId,Intent.ACCESS);
  ObjectActionReprBuilder repBuilder=ObjectActionReprBuilder.newBuilder(getResourceContext(),serviceAdapter,action).withSelf().withMutatorsIfEnabled();
  return responseOfOk(RepresentationType.OBJECT_ACTION,Caching.NONE,repBuilder).build();
}
