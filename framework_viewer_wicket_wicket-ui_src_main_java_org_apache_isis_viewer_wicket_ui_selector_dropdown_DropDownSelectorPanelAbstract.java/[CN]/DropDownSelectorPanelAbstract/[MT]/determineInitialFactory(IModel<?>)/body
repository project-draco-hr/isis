{
  return hasResolveEagerlyFacet(model) ? new Predicate<ComponentFactory>(){
    @Override public boolean apply(    @Nullable ComponentFactory input){
      return !(input instanceof CollectionContentsAsUnresolvedPanelFactory);
    }
  }
 : Predicates.<ComponentFactory>alwaysTrue();
}
