{
  final Class<?> originalClass=processClassContaxt.getCls();
  final Class<?> substitutedClass=classSubstitutor.getClass(originalClass);
  FacetUtil.addFacet(new ObjectTypeFacetDerivedFromClassName(substitutedClass.getCanonicalName(),processClassContaxt.getFacetHolder()));
}
