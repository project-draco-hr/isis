{
  LOG.debug("saving naked class: " + cls);
  String statement="insert into " + table + " ("+ idColumn+ ", "+ nameColumn+ ") values ("+ primaryKey(cls.getOid())+ ",'"+ cls.getName()+ "')";
  connector.update(statement);
  cls.setResolved();
}
