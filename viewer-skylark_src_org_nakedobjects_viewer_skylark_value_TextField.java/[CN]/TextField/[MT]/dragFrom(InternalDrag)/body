{
  Location at=drag.getSourceLocation();
  if (isOnResize(at)) {
    isResizing=true;
  }
 else {
    if (canChangeValue()) {
      isResizing=false;
      cursor.cursorAt(at);
      resetSelection();
    }
  }
  markDamaged();
  return null;
}
