{
  if (canChangeValue() && !isSaved) {
    isSaved=true;
    run(new BackgroundTask(){
      protected void execute(){
        save();
        getParent().updateView();
        invalidateLayout();
      }
    }
);
  }
}
