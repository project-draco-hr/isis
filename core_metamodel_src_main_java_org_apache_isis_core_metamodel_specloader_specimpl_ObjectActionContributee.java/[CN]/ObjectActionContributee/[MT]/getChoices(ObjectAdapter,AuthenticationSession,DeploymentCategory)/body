{
  final ObjectAdapter[][] serviceChoices=serviceAction.getChoices(serviceAdapter,authenticationSession,deploymentCategory);
  return removeElementFromArray(serviceChoices,contributeeParam,new ObjectAdapter[][]{});
}
