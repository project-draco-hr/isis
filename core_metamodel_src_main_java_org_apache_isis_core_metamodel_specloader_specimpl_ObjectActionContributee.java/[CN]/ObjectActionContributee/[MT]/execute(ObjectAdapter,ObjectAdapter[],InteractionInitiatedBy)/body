{
  setupActionInvocationContext(contributee);
  setupCommandTarget(contributee,arguments);
  return serviceAction.execute(getServiceAdapter(),argsPlusContributee(contributee,arguments),interactionInitiatedBy);
}
