{
  setupActionInvocationContext(targetAdapter);
  setupCommandTarget(targetAdapter,arguments);
  return serviceAction.execute(getServiceAdapter(),mixedInAdapter,argsPlusContributee(targetAdapter,arguments),interactionInitiatedBy);
}
