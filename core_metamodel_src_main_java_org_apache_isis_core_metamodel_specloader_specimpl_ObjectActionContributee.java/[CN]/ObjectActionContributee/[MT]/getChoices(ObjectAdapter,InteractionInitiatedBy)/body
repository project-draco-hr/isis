{
  final ObjectAdapter[][] serviceChoices=serviceAction.getChoices(serviceAdapter,interactionInitiatedBy);
  return removeElementFromArray(serviceChoices,contributeeParam,new ObjectAdapter[][]{});
}
