{
  while (response == null) {
    try {
      wait();
    }
 catch (    InterruptedException e) {
      LOG.error("wait (getResponse) interrupted",e);
    }
  }
  Response r=response;
  response=null;
  notify();
  return r;
}
