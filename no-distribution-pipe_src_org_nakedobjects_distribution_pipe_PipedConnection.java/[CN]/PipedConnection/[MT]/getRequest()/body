{
  while (request == null) {
    try {
      wait();
    }
 catch (    InterruptedException e) {
      LOG.error("wait (getRequest) interrupted",e);
    }
  }
  Request r=request;
  request=null;
  notify();
  return r;
}
