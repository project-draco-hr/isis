{
  final List<ObjectAdapter> serviceAdapters=getResourceContext().getPersistenceSession().getServices();
  DomainObjectListReprBuilder builder=DomainObjectListReprBuilder.newBuilder(getResourceContext()).usingLinkToBuilder(new DomainServiceLinkToBuilder()).withSelf(getSelfRef()).withAdapters(serviceAdapters);
  return builder;
}
