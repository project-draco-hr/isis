{
  if (this.version.different(version)) {
    throw new ConcurrencyException(version.getUser() + " changed " + titleString()+ " at "+ DATE_TIME.format(version.getTime())+ " ("+ this.version+ "~"+ version+ ")");
  }
}
