{
  if (this.version.different(version)) {
    throw new ConcurrencyException(modifierBy + " changed " + specification.getShortName()+ " object ("+ titleString()+ ") at "+ modifiedTime+ " ("+ this.version+ "~"+ version+ ")");
  }
}
