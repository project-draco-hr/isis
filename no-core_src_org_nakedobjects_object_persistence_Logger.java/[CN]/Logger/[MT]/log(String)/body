{
  if (file == null) {
    try {
      file=new PrintStream(new FileOutputStream(fileName));
    }
 catch (    IOException e) {
      e.printStackTrace();
    }
  }
  file.println(message);
  file.flush();
}
