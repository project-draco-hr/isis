{
  if (stream == null) {
    try {
      stream=new PrintStream(new FileOutputStream(fileName));
    }
 catch (    IOException e) {
      e.printStackTrace();
    }
  }
  stream.print(time() - start);
  stream.print("  ");
  stream.println(message);
  stream.flush();
}
