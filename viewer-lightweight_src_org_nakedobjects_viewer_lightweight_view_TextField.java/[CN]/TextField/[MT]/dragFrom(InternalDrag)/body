{
  Location at=drag.getRelativeOrigin();
  if (isOnResize(at)) {
    isResizing=true;
  }
 else {
    if (canChangeValue()) {
      isResizing=false;
      cursor.cursorAt(at);
      resetSelection();
    }
  }
  redraw();
}
