{
  NakedObject testObject=new TestObject();
  SnapshotBuilder builder=new SnapshotBuilder(testObject);
  Element e=builder.toXml();
  DomSerializer serializer=new DomSerializerCrimson();
  String xml=serializer.serialize(e);
  assertEquals("",xml);
}
