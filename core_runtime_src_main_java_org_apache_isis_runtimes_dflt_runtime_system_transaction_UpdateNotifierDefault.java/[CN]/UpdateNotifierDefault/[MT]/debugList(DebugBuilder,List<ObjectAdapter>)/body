{
  debug.indent();
  if (list.size() == 0) {
    debug.appendln("none");
  }
 else {
    for (    final ObjectAdapter adapter : list) {
      debug.appendln(adapter.toString());
    }
  }
  debug.unindent();
}
