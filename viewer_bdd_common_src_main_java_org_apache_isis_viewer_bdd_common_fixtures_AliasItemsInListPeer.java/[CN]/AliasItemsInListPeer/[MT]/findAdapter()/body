{
  for (  final ObjectAdapter adapter : collectionAdapters()) {
    if (!titleMatches(adapter)) {
      continue;
    }
    if (!typeMatches(adapter)) {
      continue;
    }
    return adapter;
  }
  return null;
}
