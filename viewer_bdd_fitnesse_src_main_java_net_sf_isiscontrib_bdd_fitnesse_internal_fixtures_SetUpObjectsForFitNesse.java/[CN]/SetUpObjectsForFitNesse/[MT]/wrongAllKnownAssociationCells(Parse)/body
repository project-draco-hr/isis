{
  getPeer().forEachAssociation(new AssociationVisitor(){
    @Override public void visit(    OneToOneAssociation association,    int i){
      final Parse cell=FitnesseUtil.cell(row,i);
      if (cell != null) {
        wrong(cell);
      }
    }
  }
);
}
