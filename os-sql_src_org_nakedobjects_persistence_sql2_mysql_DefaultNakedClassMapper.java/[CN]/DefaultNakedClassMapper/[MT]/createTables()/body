{
  TypeMapper types=TypeMapper.getInstance();
  db.update("create table " + table + " ("+ idColumn+ " "+ types.id()+ " NOT NULL UNIQUE, "+ nameColumn+ " "+ types.typeFor(TextString.class.getName())+ " UNIQUE, "+ reflectorColumn+ " "+ types.typeFor(TextString.class.getName())+ ")");
}
