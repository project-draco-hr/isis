{
  String number=getNumber();
  if (number == null) {
    return "";
  }
 else {
    int len=number.length();
    if (len > 5) {
      return "********************".substring(0,len - 4) + number.substring(len - 4,len);
    }
    return number;
  }
}
