{
  return new Function<Category,ToDoItemsByCategoryViewModel>(){
    @Override public ToDoItemsByCategoryViewModel apply(    final Category category){
      return new ToDoItemsByCategoryViewModel(category);
    }
  }
;
}
