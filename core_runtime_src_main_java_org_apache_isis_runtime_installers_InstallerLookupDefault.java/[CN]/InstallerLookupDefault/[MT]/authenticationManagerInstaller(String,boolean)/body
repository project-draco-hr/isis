{
  return getInstaller(AuthenticationManagerInstaller.class,requested,SystemConstants.AUTHENTICATION_INSTALLER_KEY,useNoOp ? NoopAuthenticationManagerInstaller.class.getName() : SystemConstants.AUTHENTICATION_DEFAULT);
}
